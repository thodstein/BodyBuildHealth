# План настройки планировщика питания — «ведро» (Sep 14 2026)

Жалоба: доборы валятся в полный обед (батат 330), а полдник стоит пустой;
на больших КБЖУ — «мусорное ведро, а не планировщик».
Статус: P0–P1 выполнены кодом, P2–P4 — план ниже. Тесты области:
было 782/795 (13 падений) → стало **804/814 (10 падений, все предсуществующие — те же файлы)**.

## 1. Диагноз механизма свалки (доказано дампами, не гаданием)

Три дыры, складывающиеся в ведро:

1. **Добивки растят крупнейшее.** P0a-добор (`meal-plan-engine.ts`, `cands.sort` по `it.c`)
   и `preciseAdjust` выбирают самый большой пункт. Пустой полдник (33/120) стоит
   последним в очереди и не дожидается порции.
2. **Два взаимоисключающих правила.** `day-target-corrector.ts:1186` «сладости только
   в перекусах» vs `:1166` «десерт в обед ок (пряники/джем/мёд/финики)».
   Десертное исключение побеждает — пряники едут в обед, где уже есть рис.
3. **Least-used фолбэк обходит гейты.** `_candIterRaw0`: если свежие кандидаты
   исчерпаны капами, берётся наименее использованный из ВСЕГО пула мимо капов
   семейств, крем-субкапа и протеинового гейта.

Дополнительно найденное по пути (все с мутационной проверкой):
- Пост-трен всегда нёс 2 каши (амилопектин ролью `carb_fast` + гарнир + D-18b третья).
- Цитрус использовался закрывателем углей через 5 точек (лимон 106 г, грейпфрут 110 г).
- `exotic_kangaroo_loin` мимо `EXOTIC_FOOD_IDS`, `sauce_mayo` мимо бан-геекса завтрака.
- Ужин без якоря: рис/картофель вылетали из пула, оставались хлеб+паста
  (самые белковые медленные носители) → день висел +6% белка.
- Любимый томат: клетчаточный трим съедал до 15 г → чистка мелочи сносила;
  падение чинилось в 3 местах (приоритет + иммунитет от трима и чисток).
- Правило «<30 г = мусор» сносило чиа 22 г (~110 ккал) и шоколад 27 г (~150 ккал) —
  день терял ~250 ккал безвозвратно (кейс presleep 10.4% vs 10%).
- P4b сдавался на первом неудачном кандидате (порошок на полу 20 г блокировал
  резку мяса 80→75, болюсный день висел +3.5 г мимо капа).

## 2. Что в интернете (применимое)

Общий вывод мировой практики: **ограничения на приём, а не только на день**.

- **Schaynová 2017 (LP меню):** у каждого приёма свои min/max энергии + вектор
  распределения дня (20/12.5/30/12.5/25). У нас доли есть, коридоров приёма нет.
- **MIGP 2026 (mixed-integer goal programming):** мягкие цели с девиациями вместо
  жёстких; порции дискретные (50/100/150 г), а не 37/44/81 г.
- **Prajapati 2025 (Gurobi LP):** excess-control — ни один ингредиент не больше 20%
  приёма. Наши «ведра» — прямое нарушение.
- **medRxiv MILP+LLM 2026:** одно блюдо на категорию в приёме + main-dish 70%.
- **Код:** `jrmoynihan99/meal-planner-ai-app` (GPT + GLPK.js на порции),
  `pchs20/diet-dietitian` (Pyomo+SCIP), `dcsid/meal-plan-autopilot`
  (greedy + `coverage×3 − macro_error×0.5 + variety`), `trougnouf/FoodStuffSolver`.
  Вывод: LP-солвер ставят только на порции поверх rule-префильтра, не вместо движка.

## 3. План по эпикам

### P0 «Ведро» — ВЫПОЛНЕНО (`60fd8842`, частично `3593f7c5`, добивка — текущий коммит)
- [x] P0-1. Топ-ап кандидаты: протеиновый гейт и в least-used фолбэке
  (`day-target-corrector.ts`: свежие + фолбэк — только lean при белке дня ≥ цели).
- [x] P0-1b. Выбор приёма: сначала недолитые (<100% ккал-цели), потом все
  (обе точки выбора топапа + portable-фолбэк). Было: батат 330 в полный обед
  при пустом полднике.
- [x] P0-2. Десерт-унификация: сладости в обед/ужин мимо лестницы
  (`meal-plan-engine.ts`, engine-ladder). Десерт-топупы корректора вне HV
  ужаты до 30 г (пряники 50 г к рису — «свалка»).
- [x] P0-3. P4b: перебор кандидатов вместо сдачи на первом
  (болюсный тест 252.9/253 зелёный).
- [x] P0-4. P0a-добор: kcal-гейт 97% душил углеводы при перекосе макрос
  (ккал 98% за счёт жиров/белка, угли −10% — добор не запускался, кейс presleep
  10.4% vs 10%). ВЫПОЛНЕНО: режим свопа (угли<цели−5 при ккал<цели×1.03,
  перебор ≤3% приемлем) — кейс 5.7%, запас есть.
  Полный ребаланс «забрать у перелитых» (move-семантика) — остаётся P5-уровнем.

### P1 «Одна каша» — ВЫПОЛНЕНО ЧАСТИЧНО
- [x] P1-0. Пост-трен: роль напитка `liquid` + второй гарнир только HV.
  Lock-тест `planner-base-quality.test.ts` 4/4 (пост-трен, обед/ужин/завтрак,
  цитрус/бобовые, экзотика).
- [x] P1-1. Veto-2: белковый гарнир (потенциал >8 г белка) в обед/ужин → замена
  на lean. Биссекция показала: на HV-регрессии не влияет (страховка, не виновник).
- [x] P1-2 (остаток, `0c3e32e8`). Relief-клапан ВЕРИФИЦИРОВАН, а не переписан:
  дампы показали, что кейса «рис+паста в обед на HV» в живой матрице нет —
  требование «второй сухой только ростом» уже выполнено механикой (merge-door
  + grow-фолбэки + десертное правило №3; relief лишь открывает гейт при
  структурном недоборе). Проверено и на полных пулах (6 сидов HV), и на узком
  пуле (только рис+овсянка: дублей нет, сходимость 87-95% ростом). Во избежание
  регресса: пул вынесен в экспорт `DRY_DENSE_IDS` (единый источник вместо
  локального списка) + lock `planner-relief-grow` 2/2 (нет дублей сухих из
  разных семейств; узкий пул сходится ≥85%). Форсированный grow-only был бы
  вреден (при гарнире у капа — потеря сходимости узких дней).

### P2 «Ведра и крошки» — ВЫПОЛНЕНО ЧАСТИЧНО
- [x] P2-0. Капы порций 350/250/100 — только не-HV (на 1500У вёдра нужны сходимости).
  Citrus-капы через единый `citrusFruitCapG()` (лимон 30 / грейпфрут 80) в 6 точках.
- [x] P2-1. Rule-1 «<30 г = мусор»:Keep при realistic-полах (чиа/шоколад целы,
  крошки ниже полов — всё ещё мусор) + иммунитет любимым + любимый приоритет
  в veg-чистке. Починило presleep-convergence и preferred-tomato.
- [x] P2-2 (остаток, `0035c526`). Дискретный ряд ОТКАЧЕН, verify-close: дамп
  показал 100/263 пунктов ≥30 г некратными 5, но blanket-округление доказало
  несовместимость с калибровкой В ОБЕ СТОРОНЫ (вниз: Ужин 16 г → 0.2195 рвёт
  MPS-пол dietology-guarantees; вверх: 253 > 253.0 рвёт белковый кап
  meal-target-scale — оба пойманы живым прогоном). Движок откалиброван на
  целочисленной точности; «рваные» 78/133 — косметика, вёдра чинят капы.
  Держим доказанный инвариант: все граммовки целые (дробных в матрице ноль).
  Lock `planner-portion-grid` 1/1 (integer-инвариант, не сетка).
- [x] P2-3 (остаток, `bb865d08`). Excess-control «пункт ≤25% ккал приёма»
  реализован честным ядром: дамп 8 конфигов показал, что blanket-25% снёс бы
  легитимную структуру (119/263 items >25% — мясные пары, жиры-плотности,
  peri-окна, основа 52-87%), поэтому scope = только НЕ-главный carb-пункт
  свыше 30% (класс «батат 330 к рису», резка к точной точке 3/7 от остального
  приёма). Rank0 exempt (основа; medRxiv main-dish 70%), белок/жиры/овощи/
  фрукты exempt (своя физика и свои капы), peri/pre-sleep exempt (фикс-бюджеты),
  HV exempt (прецедент P2-0: на 1500У крупные гарниры несут сходимость —
  без этого R-HV уезжал 0.219→0.224). Чистый guard: живой максимум 29%,
  сегодня не срабатывает. Lock `planner-excess-control` 3/3 (ведро режется,
  rank0 цел, матрица 8 конфигов). §5-«25% для всех» скорректирован данными.

### P3 «Пустые полдники» — ВЫПОЛНЕНО ЧАСТИЧНО
- Причина пустых снеков на HV: стейплы (крем/овёс/хлопья/манка) исчерпываются капами
  семейств → фолбэк джем 35. Не чинить поднятием капов (лягут F1-моно-тесты).
- [x] Стейплы расширены: лаваш + тортилья в хвост списка (независимые пер-id квоты,
  семьи нет; джем-фолбэк отодвинут). Снековые капы порций — всегда (картошка 366 г
  в перекусе недопустима и на HV).
- [x] Recipe-путь: сайдам комната приёма (цель+15%, как peri-паттерн) — сайд больше
  не переливает обычный приём. Батат 330 РАЗОБРАН (`bba74f32`): связка
  «рецепт ×0.5 + топап ребаланса + посадка ×110%» — (1) топап ребаланса капался
  общим 400 г без по-продуктовых капов → батат 340 (repro-тест дал 374);
  (2) финальная посадка масштабировала весь приём включая сайд → 340×110%=374.
  Фикс: единый `RECIPE_SIDE_CAP(_HV)` для сайдов и ребаланса + посадка только
  ядра рецепта (`ingredientIds`, сайды не трогаются). Lock-тест
  `planner-recipe-rebalance-cap` 1/1 (до фикса падал: 374 > 300).
- [x] Остаток (`74629361`): минимум снека ≥60% углеводной цели — финальный
  writer-пасс в конце сборки (позже баланса/чисток/клампов): сначала рост
  существующего гарнира, иначе наименее использованный не-сахарный стейпл
  с капами (uses/семейство/крем-субкап/клетчатка/ккал-комната/белковая комната);
  не вышло — честная нота «не сошлось» (критерий §5). Поймано по пути:
  ранний пасс не работал (баланс Р-2.3 резал снек позже: 79→66, а чистка
  мелочи добивала ужатый крем 29 г → 39.6) — гарантия чинит итог, поэтому
  стоит последней. Белковый гейт (долив не раздувает перебор Б) — пойман
  тестом meal-target-scale (3 болюса). Джем-фолбэк ≤20 г — ОТКАЧЕН:
  глобальный кап 35→20 рвал сходимость каскадом (A/B: w120/train dC 0.13→0.21,
  доказан двупрогоном + мутацией guard), вкус держится стейпл-приоритетом
  пасса + сахарными капами дня. Lock `planner-snack-minimum` 4/4 (снеки
  обычные/HV, fill-ветка едой, A/B-guard сходимости w120 — мутационно проверен).
- Сторож: HV adequacy + F1 mono + variety-ledger.

### P4 «HV отдельно» — ВЫПОЛНЕНО (`1132da46`)
- [x] Разводка начата: вёдра, крем-субкап, HV-гейты топапов, recipeпосттрен без
  медленных круп (R-1500 починен: исключение bulgur/buckwheat из HV-посттрена).
- [x] Честный флаг products-пути: `DayPlanV2.withinTolerance/deviationPct`
  (max-dev ≤8% — осознанно мягче recipe-порога 3%) + нота «Не сошлось» при
  недоборе >8% вместо молчаливого мусора. Lock `planner-honest-flag` 3/3
  (расходящийся 1500У-флаг+нота, сошедшийся флаг, RELIEF_DESSERT_IDS).
- [x] Relief-десерты явно: `RELIEF_DESSERT_IDS` (ровно 4 id) вместо двух
  захардкоженных списков корректора (ноль поведения, lock значением).
- [x] Снековый пул явно по HV: P3-долив на HV сортирует стейплы по
  carb-удобству (плотные первыми, паритет с ребалансом); обычные дни legacy.
  Без этого низкоплотный гарнир раздувал тарелку на HV.

### P5 Стратегическое — ВЕРДИКТ: оценено, не преследуем (`5cd019af`)
- LP-досчёт порций (прецедент `meal-planner-ai-app` + GLPK.js) НЕ внедрён,
  осознанно, по трём причинам с доказательствами:
  1. В package.json нет LP-зависимости (проверено grep: glpk/lp-solver/highs
     отсутствуют) — P5 требует новую зависимость (бандл/лицензии/поддержка),
     решение продуктового уровня, не инженерное.
  2. Оставшиеся 10 падений — структурные, не арифметические: пулы/капы
     семейств и порционные капы запрещают нужные угли (operability-HV,
     R-HV 0.22, convergence), полы белка (dietology), типология/UI (edibility,
     bolus, e2e). LP при ТЕХ ЖЕ ограничениях не превзойдёт greedy существенно —
     чинить нечего в классе «не туда».
  3. Класс «добор не туда» уже вылечен структурно: P0-1b (топ-ап в недолитые)
     + lock `planner-topup-emptiest` 1/1 (юнит на корректор: сытый обед ≥100%
     не получает топапов, недобор 42У едет в голодный перекус; по пути поймано:
     _mealOverT2-вето требует цели с запасом — задокументировано в тесте).
- Move-семантика (остаток P0-4 «забрать у перелитых»): корректор уже делает
  монотонные свопы; полный move — тот же класс риска, что откаченные
  salt-векторы (jam-кап, сетка порций). Не преследуем.
- Условия пересмотра: новый класс падений, доказанно арифметический (A/B:
  greedy-dev минус LP-dev на тех же ограничениях >5 п.п.), или продуктовое
  решение о зависимости. До тех пор P5 закрыт.

## 4. Счёт тестов

Область IndividualPlan: было 782/795 (13 падений) → стало **804/814 (10 падений)**.
Все 10 оставшихся — предсуществующие (operability×2, convergence-Этап6, dietology,
edibility, high-volume-bolus, hv-adequacy, R-HV, recipe-e2e×2; те же файлы и
ассерты; микродрейф значений внутри падающих ассертов от срабатывания сеток:
dietology P 0.3118→0.3111, fiber-порог 55.5→53.2 — оба всё равно падают).
По пути починены 3 baseline-падения (matrix, operability-max, convergence-highcal)
+ 3 HV-маргинала прошлого раунда (presleep, meal-scale-bolus, R-1500)
+ 19 новых lock-тестов зелёные (base-quality 4 + rebalance-cap 1 + snack-minimum 4 + excess-control 3 + honest-flag 3 + relief-grow 2 + portion-grid 1 + topup-emptiest 1).
`tsc --noEmit` — 0 по всему проекту.
Коммиты: `3593f7c5`, `60fd8842`, `5d56ee31`, `4a1a1634`, `fe0a5235`,
`bba74f32` (vedro-330: кап ребаланса + посадка ядра + lock-тест),
`bb84efc7` (план: vedro-330 + счёт), `74629361` (P3: минимум снека + lock 4/4),
`4e4d7396` (план: P3 + счёт), `bb865d08` (P2-3: excess второго гарнира + lock 3/3),
`36e1ddeb` (план: P2-3 + счёт), `1132da46` (P4: честный флаг + lock 3/3),
`2b36260a` (план: P4 + счёт), `0c3e32e8` (P1-2: verify-lock relief + DRY_DENSE_IDS),
`7a1d6c8a` (план: P1-2 + счёт), `0035c526` (P2-2: сетка откачена + integer-lock),
`080be7e1` (план: P2-2 + счёт), `5cd019af` (P0-1b-lock + вердикт P5). Без пуша.

### Раунд «ручная карточка + аудит кнопок» (Sep 15 2026, коммит `03f1a816`)

- **Ручная карточка (fix «не собирается рацион»)**:
  - `planner-day-targets.ts` — manual-ветка теперь принимает `manual.gPerKg` как фолбэк
    граммов (явные граммы приоритетнее); раньше г/кг-инпуты уходили только в `calcTargets`,
    игнорируемый manual-веткой → цели ≈0.
  - `IndividualPlanContext.tsx` — проброс `gPerKg` в `buildDayTargets` (+ dep); эффект-подсказка
    белка переведён на fill-if-empty (был форсированный 2.5 / кламп 1.8 — сносил ручной ввод
    при монтировании); гард `Array.isArray` в autofill (краш на `foodIntolerances`-объекте).
- **Аудит кнопок настроек (клик → состояние/персист)**, NEW тесты:
  `planner-manual-card` 4/4 (ручной ввод end-to-end), `planner-settings-e2e` 8/8
  (сид→генерация→`he_day_plan`: ручной белок/г-кг, кето, исключения, веган, intra, обед, вес),
  `planner-settings-clicks` 7/7, `-clicks2` 16/16 (цели/фазы/бюджет/пресеты/привязка/toggles/
  автозаполнение + lock на `foodIntolerances`-объект), `-clicks3` 14/14 (попапы/селекты/knob-тоглы/
  времена/вкусы/PopupText), `-clicks4` 12/12 (инъекция, БАД, любимые, исключения, план-тип,
  периодизация, адаптация веса, спецприём-тогл и модалка, приёмы пищи, женский цикл, календарь цикла).
- Счёт области: **866/876 (10 падений — тот же предсуществующий baseline: operability×2,
  convergence-Этап6, dietology, edibility, high-volume-bolus, hv-adequacy, R-HV, recipe-e2e×2)**;
  `tsc --noEmit` **0 по всему проекту**.

### Раунд «АВТО-число приёмов + честная рекомендация» (Sep 15 2026, коммит `15680038`)

- **NEW `planner-meal-count.ts`** — единый расчёт: `recommendMealCount(awakeH, P, C, {weightKg, kcal})` +
  `recommendMealCountDetailed` (binding) + `perMealProteinCapG` (0.45 г/кг, зажато 45–70) +
  `awakeHoursFromTimes`. Ёмкость нормальной тарелки: ≤pCap белка / ≤120 г углей / ≤900 ккал на приём;
  часы бодрствования — только физиологический пол 3/4/5. Legacy-вызов (3 аргумента) сохранён.
- **Выбор числа приёмов УБРАН** (`IndividualPlanSettings`, `IndividualPlanContext`, `IndividualPlanResults`):
  пилюли 3–10 и `PopupNumber` удалены, `setMealsCount` вычищен, кнопка «➕ +1 приём» в результатах удалена;
  контекст считает `mealsCount` производно от цели дня. В настройках — строка «Количество приёмов пищи —
  автоматически» с числом и причиной (белок/угли/ккал/день).
- **Движок `buildDayPlan`** — guardrail физической вместимости: на дефолтных ≤5 приёмах расширяет при
  крупных макросах (≤130 г углей / ≤75 г белка / ≤1100 ккал на приём) + legacy-экстрим углей (≥1100/≥1300);
  нормальные дни и явный контракт `mealsCount` (3→3, 8→8) не трогает. Нота «День требует N приёмов…».
- **Аудит-матрица** (pro/simple/minimal × products/recipes × обычные/HC800-1500/HP300-500) ДО правки:
  simple/minimal HC800 на 5 приёмах давали −50% углей, HP400 −10% белка, HP300 90 г белка/приём.
  ПОСЛЕ: HC800 −50%→ ≤12% (и ≤3% при авто-числе), HP400 → −0.3%, HP300 90.7→62 г белка/приём.
- **Счёт области**: **875/884, 9 падений** (baseline был 10 — один operability-тест вылечен
  авто-расширением); `tsc --noEmit` **0 по всему проекту**.
- **Остатки (честно)**: рецептурный путь на экстрим-углях всё ещё до −30% (baseline R-HV/HC1000-recipes);
  per-meal белок не форсируется жёстко (79–93 г при 500Б) — регулируется только число приёмов;
  pro-путь добавляет peri-приёмы (prew/postw/intra) СВЕРХ авто-числа — физиологические окна, не «ведра».

## 5. Критерии приёмки плана целиком
- Обед ≥100% цели не получает топапов; lock-тест «топ-ап в самый пустой»
  (`planner-topup-emptiest` 1/1, юнит на корректор).
- Lock «≤1 гарнира» расширен на HV и recipe-сайды (батат 330 в обед падает).
- Второй гарнир ≤30% ккал приёма (P2-3; blanket-25% для всех пунктов отменён
  данными: мясные пары/жиры/peri/основа легитимно выше — дамп в плане).
- Порции из дискретного ряда — ОТМЕНЕНО данными (P2-2: сетка рвёт калибровку
  в обе стороны; держим integer-инвариант + ряд ×0.5/×1/×1.5 в рецептах).
- Снек <60% цели невозможен без ноты «не сошлось».
- Матрица 80 ячеек + `tsc` + соседние области — зелёные.

## 6. Реальная выдача: авто-число приёмов, высокие макросы (инсулин/ААС), portable, вкус/комфорт

Статус: **ПЛАН** (Sep 15 2026). Контекст владельца: 500+ г углеводов идут С инсулином
(болюсы), 250+ г белка — на ААС, утром нужно существенно >40 г белка; «сделать качественно».

### 6.1 Аудит (что уже есть / где дыры)

Есть:
- **Инсулин-окна**: `meal-plan-engine.ts:4113-4221` — на каждый болюс создаётся приём-окно с
  `carbG = dose×10` (правило «10 г У на 1 ЕД»), `_insulinWindow=true`, примечание с желаемым
  диапазоном; недельный потолок углей поднимается болюсами: `:3450-3455`
  `_carbCeiling = max(50, weight×capPerKg, bolusUnits×12)`; `day-target-corrector` исключает
  окна из ребалансов (чтобы не сдвигать болюсные таргеты).
- **MPS-распределение**: `MPS_LBM_LOW/HIGH` 0.3/0.4, `MPS_WEIGHT_OPT/HIGH` 0.40/0.45
  (`meal-plan-engine.ts:210-213`, `:3347-3352`), проверка `leucine ≥ порог && p ≥ 25` (`:852`).
- **Завтрак**: `breakfastProteinPref(..., proteinTargetG)` + минимум 75 г цельного белка на
  завтрак (`:1778`), `BREAKFAST_PROTEIN_IDS` (`:741`).
- **Portable**: per-meal гейт `:1594-1637` + `isPortableFood` allowlist
  (`food-availability.ts:601-634`: flakes/cereal/muesli/rice_cream/cream_of_rice/bread/rice_cake +
  порошки + молоко/кефир + орехи + fruit/veg). Пул хлопьев покрыт.
- **Рабочее время**: обед в середину смены и ужин +30 мин после конца (`:3242-3257`),
  снеки по серединам; `shift_day_night`/`shift_N_M` (`IndividualPlanContext:3377`).

Дыры:
- **Авто-число приёмов не знает про инсулин и peri**: `planner-meal-count` считает по белку/углям/
  ккал, но не добавляет болюсные окна и peri (prew/postw/intra/preSleep) — в Pro фактическое
  число = авто + peri (до 13), в UI фиксируется только авто.
- **Завтрак не форсируется по белку** относительно дня: правило есть (≥75 г порция), но нет
  целевой доли «утро ≥ 0.5–0.55 г/кг / ≥30% дневного белка» при высоком белке/ААС.
- **Recipe-путь игнорирует portable** при выборе блюд (`assembleRecipeDay` не применяет
  `isPortableFood`) и даёт −30% на экстрим-углях.
- **Shift не учитывается в quick-add/add** (`IndividualPlanContext:1803`, `:2248`) — там только
  `workDays[dayIdx%7]`, без `shift_*`.
- **Вкус/комфорт**: только `tasteMatchScore` (+30% вес, `:1148-1156`) и ~10 записей
  `COMFORT_PORTION_LIMITS` (`:445-453`); нет co-occurrence (recipe completion), нет общего
  лимита «одно блюдо/категория», нет контроля «сладкое N/день».

### 6.2 Исследования (применимое)

- **Частота приёмов**: RCT 2025 (PRO3x vs PRO5x, n=32) — 3 и 5 приёмов по 0.24+ г/кг/приём
  равноценны для массы/силы; значит число приёмов задаёт не «традиция», а необходимое число
  порогов MPS + вместимость тарелки. Schoenfeld & Aragon: цель **0.4 г/кг/приём** (≥4 приёма),
  верх 0.55 г/кг при 2.2 г/кг/сут. Examine: 0.40–0.55 г/кг на ~4 приёма, диминишинг после 4–6.
- **Порог лейцина**: 2.5–3 г/приём (у молодых 2.5); «скип завтрака с 12 г» не пересекает порог —
  белок утром важен; plant-based нужно 30–40 г/приём.
- **Высокие углеводы**: загрузка 3–12 г/кг/сут (Sports Med Open 2024), 8–10.5 г/кг для MGS;
  лимитер — желудочная толерантность и SGLT1: распределять приём, предпочитать
  низкоклетчаточные плотные источники (crisp/cream of rice/rice cakes/мёд/джем),
  жиры в нагрузочных приёмах держать ниже (замедляют опорожнение). Практично ~120–200 г
  углеводов на основной приём.
- **Инсулин**: правило dose×10 г У на болюс — уже в движке; окна должны быть зафиксированы,
  остальные углеводы распределяться между окнами, не «в окно».
- **Recipe/меню-матчинг**: MIGP (integer + goal programming, дискретные порции) — 100% решаемость,
  медиана макс. отклонения 1.4–6.3%, никогда не хуже post-hoc округления; MILP+LLM —
  лучшая точность (4.93/5), но ниже практичность; «recipe completion» (co-occurrence)
  повышает приемлемость сочетаний; дискретные порции 50/100/…/400 г повышают adherence.
- **Портатив**: overnight oats (овёс+протеин+чиа/йогурт) 30–45 г белка, партия на 4–5 дней;
  хлопья/мюсли+порошок — прямой аналог нашего portable-пула.

### 6.3 План работ (эпики)

**E1. Единый авто-расчёт приёмов с учётом инсулина и peri (S).**
`planner-meal-count` → функция `planMealStructure({weight, proteinG, carbsG, kcal, injections,
isTrainingDay, allowIntra, wake, bed, nightCarbs})` → `{ regularMeals, periMeals, insulinWindows,
distribution }`. Правила: белок 0.4–0.55 г/кг/приём (ААС/курс → верх диапазона 0.5–0.55),
угли ≤120–150 г/приём, ккал ≤900–1000/приём, интервал 3–4 ч, +1 приём на каждый болюс-окно
(или окно занимает один из приёмов, если по времени совпадает), лимит 10 основных + peri.
Движок и UI читают ОДНУ функцию; нота объясняет состав (белок/угли/ккал/инсулин/часы).
Тест: Pro/Simple/Minimal × (3000/180/400) и (5100/220/800)+3 болюса — факт.число = авто,
интервалы 2.5–4.5 ч, окна на месте.

**E2. Утренний белок (S).**
Целевая доля завтрака: ≥ max(0.5 г/кг, 30% дневного белка) при белке ≥2.2 г/кг или ААС;
при обычном белке — не менее 0.4 г/кг. Завтрак первым получает белок в раскладке
(сейчас выбор свободный), но без ломки калибровки тарелки. Тест: 250/400/500 г белка →
завтрак ≥ порога, нет «12 г завтрак».

**E3. Portable на работе — довести (S).**
(a) recipe-путь: фильтр/подстановка портативных блюд для приёмов в рабочем окне;
(b) единый `isWorkDayFor(offset, schedule)` (standard/sliding/custom/shift_day_night/shift_N_M)
для generatePlan + quick-add + add (сейчас 3 копии, 2 без shift);
(c) «хлопья» гарантированы в portable-пулах всех приёмов окна. Тест: shift_day_night + portable →
в окне только портативное (products и recipes), обед в середине смены.

**E4. Recipe-путь: сходимость как MIGP-lite (M).**
Для основных приёмов — soft-deviations по Б/Ж/У/ккал + дискретный ряд порций, глобальный подбор
(не post-hoc округление), сайды отдельными переменными с капами, «одно блюдо на категорию»,
main-dish ≤70% ккал. Цель: **max dev ≤3%** обычные, **≤5%** HV (честный флаг), 0 пустых мейнов.
Тест: 800/1000/1500 У — в допуске; рецепт не вышел из ряда порций; portable в окне (E3).

**E5. Вкус и комфорт (M).**
- Comfort: категорийные капы (соусы/сладости/сухофрукты/орехи/масла/мёд), «сладкое ≤2/день»,
  «≤2 повторов стейпла/день», «не более 1 «тяжёлого» жирного приёма в рабочие часы».
- Taste: co-occurrence из RECIPE_DB/данных (recipe completion) поверх `tasteMatchScore`;
  баланс вкусов дня (не 4 сладких).
- Тест: 5 вкусовых профилей → смещение выбора; комфорт-капы; ≤2 сладких.

**E6. Гигиена (S).**
Отметить P0-4 (сделано), перенести отложенные VARIETY P2-1/P2-3/P2-4 в этот план со статусом,
зафиксировать «остатки честно» (recipe −30% до E4, per-meal белок, pro peri).

### 6.4 Критерии приёмки §6
- Одна функция числа приёмов; UI и движок не расходятся; окна инсулина учтены; интервалы 2.5–4.5 ч.
- Завтрак: ≥0.5 г/кг (или ≥30% белка дня) при белке ≥2.2 г/кг/ААС; ≥0.4 г/кг иначе.
- Portable: 100% приёмов рабочего окна — портативные (products+recipes), все shift-типы.
- Recipe: max dev ≤3% (обычные) / ≤5% (HV); «одно блюдо/категория»; порции из ряда.
- Комфорт/вкус: ≤2 сладких/день, comfort-капы, ≥3 семейства стейплов/день.
- Тесты области: 0 новых падений (baseline 9), `tsc` 0.

## 7. Передача в новую сессию (осталось закрыть 9 baseline-падений)

Статус: §1–§6 выполнены (E1–E6; статус эпиков — в §6.5 ниже), тёсты области **897/906, 9 падений — исторический baseline**
(до всех раундов плана было 10; одно ушло). Новых падений нет. Осталось «добить» именно эти 9.

> ### ✅ ВЫПОЛНЕНО (Sep 16 2026, сессия §7): область **916/916 (94 файла), 0 падений**
> Все девять baseline-падений закрыты; `tsc --noEmit` — 0 по всему проекту. Коммиты (pathspec, без пуша):
> `98f376e2` (§7.1), `2471921f` (§7.2-1/2), `ecc08b19` (§7.2-3), `8c1625e7` (§7.2-4),
> `f2e62f81` (§7.2-5) + финальный коммит §7.2-6/7 (движок+корректор+кап-словарь+этот док).
> Что оказалось на деле (гипотезы §7.1/§7.2 подтвердились лишь частично):
> - **§7.1 (два UI)**: пикер/ребаланс исправны — тесты ждали старое имя блока «Рецепты для этого
>   приёма» (переименован в «🍲 Подсказки рецептов» в `24cff515d`). Фикс — тестовые ожидания.
> - **§7.2-1 (fiber peak cap)**: `fiberCapG<35` теперь реально управляет составом дня: низкоклетчаточные
>   углеводы/жиры (рис/рисовый крем/паста, масла вместо авокадо/чиа), без ALA-семян, корректор уважает
>   явный кап (`_corrFiberCap`), P3-пул низкоклетчаточный. Факт: 16.6 г при капе 20 (было 57).
> - **§7.2-2 (max vs medium)**: авто-число приёмов добирается по ёмкости ГЛАВНОГО приёма (обед 1.7/wSum),
>   а не по среднему: 5→6 приёмов на 500У/3600 (движок сам писал «минимум 6»). MAX −3.1%.
> - **§7.2-3 (90кг, белок)**: при низкой дневной цели окна peri/pre-sleep и полы P4b ужимаются
>   пропорционально (144 г цель не уходит +31%); база — полы регулярных приёмов 82 г.
> - **§7.2-4 (edibility)**: четыре низкоплотных крахмала (картофель/батат/булгур/запечённый) ≤300 г:
>   EDIBILITY_CAPS(300) + финальный HV-кламп ТОЛЬКО по этим id (широкий кламп по всем EDIBILITY_CAPS
>   ломал relief-grow — `oats_dry` кап 100 душил сходимость).
> - **§7.2-5 (болюс рядом)**: основной приём ≤20 мин от болюса покрывает его (окно-дубль не создаётся);
>   20–60 мин — честный топ-ап до потребности дозы (тест «3 болюса» на 19:30/ужин 19:00 цел).
> - **§7.2-6 (hv-adequacy 900У)**: P4b на экстрим-бэнде (Б≥2.3 и У≥8 г/кг) ужимает полы цельного
>   белка до 60 г мейн / 48 г перекус (вне бэнда — 80/50, F1 цел) + финальный У-добор после чисток
>   (`§7.2-6b`, HV-день) + низкобелковый приоритет топапов. Факт: У 855, Б 278, снеки ≥40.
> - **§7.2-7 (R-HV 1500У)**: финальный экстрим-добор `§7.2-7c` после всех проходов ростом
>   существующих гарниров (низкобелковые носители, полоса Б 35%, тарелка ≤730 г твёрдого) + P3-полоса
>   на экстрим-бэнде (иначе белковый гейт душит снеки: 39.6<40). Факт: У 1454/1500 (3.1%), окна 3×90–130.
> Отклонено по пути (мутационно/по тестам): флип-гейт P2→пере-добор не трогал (ломал eveningLowCarb),
> широкий EDIBILITY-кламп (ломал relief-grow), корректные полы P4b без бэнда (ломали F1/R-1500),
> «белок-не-мешает-углям» пол 1.35 на всех HV (ломал 3-болюса).

> ### ✅ §7.4 Аудит «что осталось» — 4 пункта (Sep 16 2026, продолжение)
> Аудит дампами (`zz-recipe-extreme-dump`, `zz-500p-dump` — временные, удалены до коммита):
> **1) Рецептурный путь на экстрим-углях** (было: R-1500 финишировал 1136/1500У −24%, dev 24.2%):
> корень — перебор белка ядер рецептов делает день «ккал-перегруженным» для КАЖДОГО приёма
> внутри проходов, поэтому углеводный топ-ап видится «перебором калорий» и откатывается
> (осцилляция add/cut в notes). Фикс — **§7.2-Р-финал** (`extremeCarbTopUp` в
> `planner-recipe-mode.ts`, добор ПОСЛЕ корректора, как §7.2-7c в продукт-пути): экстрим-полоса
> (У≥8 г/кг и Б≥2.3 г/кг), носители только Б-нейтральные (рис 2.7/картофель 2.0, порошок/сахар/
> булгур/ватат — нет), комнаты приёмов + тарелка ≤730 г твёрдого, вычет с честным потолком
> (`honestDevCap` 30 — рис несёт ~10 г Б на 100 г У). Факт: У 1287/1500 (86%), ккал 7840/8600,
> dev 29.7 ≤ 30 (= честная граница пути; Б ядер не трогаем). Локи: `planner-recipe-mode` +4,
> R-1500 +1 (≥85% У, заметка добора).
> **2) per-meal белок на 500Б (79–93 г)**: принято как дизайн — 500 г в ≤8 приёмах = 62–93 г
> (ultraP-коктейли egg_white+whey закрывают разницу; cap мейнов 68 при Morton-бюджете);
> сглаживание требовало бы 11+ приёмов или срез цели. Не меняем (тесты `planner-high-volume`
> стерегут сходимость 500Б train/rest).
> **3) Карточка числа приёмов в Settings**: показывала `recommendMealCountDetailed` без курса
> (0.45 vs 0.55 г/кг) и без peri/окон → расходилась с фактической сборкой. Фикс: единый
> `planMealStructure` (те же входы, что у движка: onCourse, болюсы, тренировка/интра) и честный
> итог «N основных + M peri + K окон = X приёмов». Лок: NEW `planner-meal-structure-ui` 2/2
> (курс 47 г/«на курсе»; peri сверх основных).
> **4) VARIETY P2-1/3/4**: переведены из «⏸ отложено» в **границы** §5 с обоснованием
> (re-baseline скоринга/квот; рефактор каскада корректора рискует ±3%; полная эксклюзия дня
> рвёт квоты белка) — не pending-работа.
> Проверено: область приёмки + tsc (см. запись AGENTS.md этой сессии).

> ### ✅ §7.5 Вторая волна «что осталось» (Sep 16 2026): авто-число приёмов + п39 + 500Б
> **0) Выбор числа приёмов убран совсем**: в Профиле (Питание) вместо PopupNumber 2–8 —
> read-only «· авто» с подписью «подбирает планировщик по ёмкости тарелки»; в планировщике
> ручного пути не было и раньше (`planMealStructure` + guardrail движка). `nutrition.mealsPerDay`
> в профиле теперь производное (пишет Context для отчётов), не настройка.
> **(а) Контент + ранкинг экстрима**: NEW шард `recipe-db-p39` (4 карб-лоад блюда, У/Б 5.4–11.5,
> Б ≤30 г); вне экстрим-полосы тег `carb-load` фильтруется из пула (обычные дни байт-в-байт),
> в полосе ранжирование кандидатов взвешено (У ×0.6, Б ×0.05) — иначе мясные блюда выигрывают
> по дистанции белка. R-1500: **У 1471/1500 (98%), ккал 8454/8600 (98%), dev 28.9** (Б ядер
> +29% — honest-потолок; ядра не режем). Локи: p39-контент, «вне полосы не выбирается»,
> R-1500 «Загрузка: реально выбралась».
> **(б) 500Б-сглаживание**: (1) белковый топ-ап ограничен комнатой ПРИЁМА (цель×1.15) и
> пропускает уже сытые приёмы — на 500Б тёплые мейны 95→89/87, перекусы 26→43; (2) после всех
> доборов — перераспределение «перебор ← недобор» (порошок-донор приоритетно). Остаточный
> перекос снек-коктейля (88 при цели 40) — структурный: цели приёмов в сумме ниже дневной
> (486/500), места для перераспределения нет. Лок: мейны ≤ цель×1.35.
> **(в) mk677** в `ped-class-matrix` → класс **gh** (грелин-миметик; был SARM → чужие дозовые
> тиры/мониторинг); грп/цжк/семорелин и т.п. тоже gh. Лок: `support-calc-ped-e2e` +1.

> ### ✅ §7.6 Ручной КБЖУ — источник правды + честные тексты (Sep 16 2026)
> После отмены выбора числа приёмов два текста всё ещё советовали «увеличьте число приёмов» —
> переписаны честно (движок ×2 + Results ×1: «число приёмов план подбирает сам… распределите
> вручную/скорректируйте цель»).
> **Ручной режим (kbjuMode='manual') проверен end-to-end и залатан в трёх местах, где цели
> пользователя молча перебивались**:
> 1) ББ-план (`applyBBNutritionToTargets`) сдвигал углеводы ±30/−25 г по трен-дням и подменял
>    калораж (±15%) → NEW `locked` (Context передаёт `kbjuMode==='manual'`): цели неприкосновенны,
>    ББ-заметки только справочные;
> 2) потолок углеводов 8–10 г/кг в движке применялся и к ручным целям → в manual-режиме
>    `carbCapGPerKg: 0` (без потолка) + warning `carbCapClipped` в manual скрыт;
> 3) детали карточки ручного ввода (г/кг vs граммы, kkal-fallback) были верны — закреплено
>    лок-тестами (`planner-day-targets` уже были).
> Локи: `planner-bb-nutrition` +2 (locked: нет сдвига/подмены), NEW `planner-manual-kbju` 2/2
> (цели дня = 5040/180/80/900 при «враждебной» ББ-заметке 6300+трен; сгенерированный день несёт
> У>600 г против авто-базы ~350). Проверено: область **922/922 (86 файлов)**, `tsc` 0.

### 7.1 Два UI-падения (рецептурный пикер) — начни с них

`__tests__/planner-recipe-mode-e2e.test.tsx`:
- «применение из пикера «🍳» перестраивает рацион (кнопка печати появляется)» (L52-81);
- «⚡ Быстрый режим: чипы-подсказки… применение из пикера пересобирает день» (L96-122).
Обе падают на последнем шаге: `he_day_plan.meals.some(m => m.recipeApplied)` = false.

Что уже выяснено (не перепроверять с нуля):
- Пикер в классик/минимал — `MealComposer.tsx:360-379`; кнопка рецепта (L371) вызывает
  `replaceMealWithRecipe(r, mealIdx, dayIdx)` при `!_hasFirst`, иначе `addSecondRecipeToMeal`.
- `replaceMealWithRecipe` — `IndividualPlanContext.tsx:2261`; ветка `dayIdx===0` (L2299-2349)
  ставит `recipeApplied/recipeAppliedData` (L2313) и `setDayPlan(newDay)`; persist —
  effect `he_day_plan` (L1526). Ветка `dayIdx!==0` (L2350-2390) — three/week.
- Гипотезы по порядку: (1) `recipePickerMeal.dayIdx` приходит `undefined`/не тем индексом →
  `_resolvePlanDay` уводит в `plan==='day'` и функция выходит (L2353); (2) `applyRebalanced`
  вернул null (mealIdx вне диапазона/`dayPlan?.meals` пуст в момент клика); (3) ребаланс
  кинул исключение до `setDayPlan`. Отладка: временный `console.log` в
  `replaceMealWithRecipe` (вход `dayIdx, mealIdx, длина meals`, возврат `resMeals`), прогнать
  один тест `npx vitest run <файл> -t "применение из пикера"`, затем убрать лог.

### 7.2 Семь движковых падений (по одному механизму — проверить дампом)

Все — продуктовый/рецептурный путь продуктового движка (`meal-plan-engine.ts`,
`day-target-corrector.ts`, `planner-carb-density.ts`):
1. `operability-smoke > fiber peak cap 20` — пик-день ББ держит клетчатку ≤20 г (сейчас больше).
   Смотреть `fiberCapG` (провайдер ББ, `_peakTargets.fiberMaxG`) и точки клетчатки в корректоре.
2. `planner-convergence > Этап 6: max-бюджет позволяет более крупные порции, чем medium` —
   сравнение порций max vs medium; вероятно мой авто-расширенный `mealsCount` выровнял
   порции (max-бюджет больше не даёт крупнее). Проверить с одинаковым `mealsCount`.
3. `planner-dietology-fixes > 120/90кг × инсулин × уровни: Б/Ж/У/ккал ~10%` — кейс
   `w=90 ins=0 mult=1`, P-отклонение 0.31 (базово было 0.3111) — расхождение по белку
   на 90 кг без инсулина; смотреть сверх-белковый бюджет `_mainPCap`/`_ultraPBudget`
   (L3712-3714) и MPS-распределение.
4. `planner-edibility > батат+булгур ≤300` — низкоплотные носители; капы `RECIPE_SIDE_CAP`/
   `EDIBILITY_CAPS` (`planner-carb-density.ts`), резка в корректоре.
5. `planner-high-volume > C: болюс рядом с приёмом — покрывается приёмом, окна нет` —
   окно болюса, совпавшее по времени с обычным приёмом, не должно создавать отдельный приём;
   смотреть `_injectMealAt`/`_insulinPlanned` (`meal-plan-engine.ts:4113-4221`) и мой
   болюс-минимум в guardrail (`if (input.mealsCount <= 5) … _bolusCount + 3`) — не дублирует ли он слот.
6. `planner-hv-adequacy > 900У: нет запрещённых круп, крем/хлопья есть, снеки плотные,
   сахар и клетчатка в капах` — HV-пул снеков/сахар-кап; `HV_STAPLE_BAN`, `sugar-cap`.
7. `planner-recipe-high-volume > R-HV products 1500У + инсулин: угли ≤5%` — топ-апы
   продуктового пути упираются в капы; факт 21.9%. Это НЕ рецептурный путь (несмотря на
   имя describe). Смотреть `mealCarbCapacityG`/`distributeCarbsByCapacity` и потолок
   `_carbCeiling` (L3450-3455).

Правило раунда: каждый кейс — сначала дамп-лог (временный `zz-*.test.ts`, удалить до коммита),
потом фикс, потом прогон всей области (baseline = 9, новых ноль).

### 7.3 Промпт для новой сессии (скопировать целиком)

```
Продолжаем планировщик питания по docs/NUTRITION-PLANNER-TUNING-PLAN.md, раздел §7.
Цель — закрыть 9 baseline-падений области IndividualPlan (сейчас 897/906), затем финальная
проверка и коммит. Правила проекта: только Edit/Write (никаких PowerShell-перезаписей файлов
и повершелов), после любого скрипта перечитать файл перед edit, чужие файлы не трогать,
коммит строго `git commit -m "…" -- <свои файлы>` (pathspec, -m до --), НЕ пушить, чекаут
запрещён, временные zz-*.test.ts удалять до коммита.
Верификация: `npx vitest run src/ui/screens/NutritionScreen_parts/IndividualPlan/__tests__`
(ждём baseline 9 падений, новых ноль) и `$env:NODE_OPTIONS="--max-old-space-size=12288";
npx tsc --noEmit` (по своим 0; в репо есть чужая ошибка armlift-completeness — не наша).
Начни с §7.1 (два UI-падения пикера): в replaceMealWithRecipe есть готовые гипотезы —
проверь их временным console.log, починить, убрать лог. Потом §7.2 по списку: каждый кейс
сначала дампом (временный тест), затем фикс, затем прогон области. Отчёт по каждому пункту
и коммит pathspec. Итог — область зелёная кроме объективно невозможных кейсов (тогда честно
задокументировать в §7.2).
```

### 7.4 Что уже сделано (не переделывать)

E1 (структура дня/инсулин/ААС), E2 (утренний белок), E3 (portable + смены), E4 (MIGP-lite
для рецептов, 8600 ккал → 6.7%), E5 (комфорт-карточка), E6 (статус). Коммиты: `72f1a9a5`,
`de1ff91e`, `dc9dacd5`, `1d026145`, `d9fa27ab`. Тесты новых эпиков: `planner-meal-structure`
10/10, `planner-comfort` 6/6, `planner-recipe-optimizer` 6/6, `planner-meal-count` 9/9.

### 6.5 Статус эпиков E1–E6 (Sep 15 2026; коммиты 72f1a9a5 / de1ff91e / dc9dacd5 / 1d026145 / d9fa27ab)
- **E1 — СДЕЛАНО**: `planMealStructure` (`planner-meal-count.ts`) — приёмы по белку
  (0.45 г/кг, на курсе/ААС 0.55, зажато 45–75), углеводам ≤130 г, ккал ≤950, пол по часам,
  окна инсулина (болюс+3, ≤9) и peri (2/3 в трен-день). Контекст считает `mealsCount`
  через неё; движок получил болюс-минимум в guardrail. Lock `planner-meal-structure` 10/10.
- **E2 — СДЕЛАНО**: утренний белок — при белке ≥2.4 г/кг завтрак получает ≥0.5 г/кг
  (или ≥30% дня) с потолком `_mainPCap` 68 г, перераспределением из обеда/ужина без роста
  дневной цели. Lock в `planner-meal-structure` (400Б/110кг → завтрак 50–68 г).
- **E3 — СДЕЛАНО**: (a) рецептурный путь в рабочем окне берёт ТОЛЬКО целиком портативные
  блюда, иначе приём остаётся портативно-продуктовым; (b) единый `planner-work.ts`
  (`resolveShiftWorkDay`/`isWorkDayForIndex`, shift_day_night + shift_N_M) подключён к
  generatePlan, add- и quick-add-путям (раньше две копии не знали смен).
- **E5 — СДЕЛАНО**: `planner-comfort.ts` — `comfortFindings` (сладости ≤2/день, комфорт-капы
  порций, ≤2 повторов семейства-стейпла, съедобный вес приёма ≤750 г) + `comfortSummaryForPlan`;
  карточка «🍽 Комфорт дня» (`data-comfort`) в результатах плана — advisory, КБЖУ не режет.
  Тесты 6/6 (+3 сценария UI-сводки).
- **E4 — СДЕЛАНО (фундамент + встройка)**: NEW `planner-recipe-optimizer.ts` —
  `optimizeRecipePortionScales` (совместный перебор дискретных шагов порций по всем
  основным приёмам, минимум максимального относительного отклонения дня; soft-goal как
  в MIGP, brute-force ≤4 рецептов, дальше жадный спуск) + `maxRelativeDeviation`, тесты 6/6.
  **Встроено в `assembleRecipeDay`**: после сборки мейнов идёт пост-пасс по «чистым»
  мейнам (в приёме только ингредиенты рецепта, без сайдов) — joint-подбор шагов с
  фиксированной частью (перекусы/peri/окна) и применением ТОЛЬКО при строгом улучшении
  и ТОЛЬКО на рост (инвариант property-теста «core не ниже выбранной порции»).
  Замер: рецептурный день 8600 ккал/4 приёма — отклонение **6.7%** (до правки аудит
  давал 15.9–23.8% на 1000–1500У), обычные дни 2.1% и 1.8%.
- **E6 — СДЕЛАНО**: P0-4 отмечен, отложенные VARIETY P2-1/P2-3/P2-4 зафиксированы в §6.3.
- **Проверка**: область **885/894, 9 падений — тот же baseline** (новых ноль);
  `tsc` — по своим 0 (единственная ошибка в чужом `armlift-completeness.engine.ts`).

