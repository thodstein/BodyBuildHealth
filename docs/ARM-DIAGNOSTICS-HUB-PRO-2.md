# Арм-диагностика ХАБ — PRO-2: без бокового скролла, без пустот + план PRO

Статус: **ВЫПОЛНЕН ПОЛНОСТЬЮ (скролл/пустоты + эпики P1–P7, 7 коммитов)**. База: `ARM-DIAGNOSTICS-PRO-PLAN.md` (выполнен 100% + D + R1).

## 0. Что сделано в этом раунде (только подача, логика/строки/aria 1-в-1)

**Жалобы:** «прокрутка в сторону» + «пустые места».

**Корень скролла:** хаб (`.train-armdiag`) осознанно исключили из правила №6
(«ленты → перенос» действовало только на `.train-arm`) — в хабе остались
`overflow-x:auto + flex-shrink:0 + nowrap` на 4 лентах: `.ad-chips`, `.ad-strip`,
`.ad-steps` (5 табов), `[data-arm='hist-strip']` (12 нед истории). На узком
телефоне/десктопе они давали боковой скролл страницы.

**Фикс (`arm-design.css`, хвост):**
- `.train-armdiag`: `.ad-chips/.ad-strip/.ad-steps/hist-strip` → `flex-wrap:wrap +
  overflow-x:visible + snap:none`, дети `flex-shrink:1` (прецедент №6, sticky
  `.ad-steps` сохраняется — wrap липкость не ломает).
- Страховка: `.ad-wrap/.ad-tabpanel → overflow-x:clip`, `.ad-mono → width:100%`.
- Все селекторы с `:is(.train-armdiag)` — префикс-чекер цел.

**Пустоты (`arm-design.css` + 2 инлайн-константы `arm-hub-panels.tsx`):**
- `.ad-wrap 12→8`, `.ad-card 16/12→12/8`, `.ad-sec 13/12→10/8`, `.ad-sec-t 10→6`,
  `.ad-head 12→8`, `.ad-banner 12→8`, чипы/полосы/история `10→4` низа,
  `video-ph 22→12`, `empty 26→16`, `cta 10/12→8/8`, `list 10→8`, `finding 6→4`,
  `volbar 8→6`; `HUB_HERO 14/16→10/12`, `HUB_SECTION_GAP 8/8→6/6`.
- Логика/строки/aria/хуки (`hub-head/hub-tabs/hub-strip/hist-strip/wp-groups`)
  не тронуты.

**Проверено:** `arm-diagnostics-hub 30/30` + `hub-tabpanel 4/4` + `arm-top-ui 16/16`
+ `apk-arm-pack 17/17` → **67/67**, `tsc --noEmit` 0 (6GB), `verify:apk-design` OK.

## 1. Аудит хаба (как есть, 1159 строк + 4 презентации)

| Узел | Состав | Проблема |
|---|---|---|
| `HubHead` | hero + score-кольцо + теги + «Как пользоваться» + scoring-строка + тост | Дубль scoring (ещё в `HubOutput` и `RecoveryTab`); HowTo — простыня |
| `HubControls` | уровень + техника + вес/WAF + пол + CTA + 5 табов | 5 stacked блоков; CTA дублируется внизу (`HubAction`) |
| `Grip` | 5 полей + Force Vector + помост + снапшоты + попытки + VBT + бенчмарки + pinch-баннер | Самый перегруженный таб; Force Vector дублируется в `Pressure` |
| `Wrist` | углы + баннер + авто-точка + видео + Kinovea + **12 точек** + bio-карточки + legacy | Пуст без выбора точек; bio-карточки = дубль `HubOutput` |
| `Pressure` | side/back + legacy + 4 точки + humerus + Force + стол-полоса + матчап + Table-IQ | Стол-полоса = дубль `HubTableStrip`; матчап/Table-IQ — отдельная вселенная |
| `Strength` | 4×F/t + динамика + асимметрия/bilateral + бенчмарки + кнопки + F/t-подсказки + история | 3 источника асимметрии (динамика/хват/bilateral) рядом, не сведены |
| `Recovery` | ACWR + tendon + лимиты + «дополнительно» + баннер + мобильность + авторег + return-to-pull + scoring + fatigue | 10 секций — второй хаб внутри хаба |
| `HubOutput` | findings + **bio-карточки (дубль Wrist)** + инъекция-превью + F/t-баннер + RSS | Дубль-узел: всё уже показано выше |
| `HubP0Panel` | аудит + причины + топ-3 + spec + дневник + 7 кнопок в одном ряду | Кнопки одного веса (инъекция = экспорт) |
| `HubTableStrip` | стол-полоса (дубль `Pressure`) + тренд | Чистый дубль |
| `HubAction` | CTA (дубль `HubControls`) + bridge-подпись + инъекция-строка | Третий CTA на экране |

**Итого дубли:** bio-карточки ×2, стол-полоса ×2, scoring ×3, Force Vector ×2,
CTA ×3, асимметрия ×3, findings (Head/Output/P0) ×3.

## 2. Интернет-синтез (что есть в мире, чего нет в хабе)

- **Техники (Wikipedia/arm-wrestling):** hook = супинация + бицепс/кисть;
  toproll = пронация + rising + back pressure + posting; press = плечо/грудь/
  трицепс, только из нейтрали, новичкам опасен (тендоны локтя). → В хабе
  техника — чип, без привязки норм/углов/рисков к технике.
- **Травмы (PMC10315927; JCT review):** спиральный перелом дистальной трети
  плеча (Holstein–Lewis 12.A1/A2, риск лучевого нерва) — самая частая травма;
  софт-тишью → возврат ~1 мес, перелом → месяцы + ниже DASH. Механика —
  высокий ротационный момент. → В хабе есть humerus-guard + side-прогрессия
  ≤10%/нед, но **нет скрининга red-flags** (боль/щелчки/онемение → стоп).
- **WAF Rules 2025 (waf-armwrestling.com):** фолы (отрыв локтя, сгиб кисти,
  касание плечом, фальстарт), ремень/strap-старт, сгонка веса, L/R — отдельные
  зачёты, возрастные группы. → В хабе Table-IQ считает фолы/срывы/ремень, но
  **нет правил-памятки** (что фол → что чинить) и привязки весогонки к классу.
- **Оптимум угла предплечья (ScienceDirect 2095034921000945):** EMG-зависимость
  от угла — обоснование для angle-бейджей (уже есть) + **норм по технике нет**.
- **Grip-миp (IronMind/GripBoard):** RT WR M 130.5 / F 77.2, Saxon Axle 133,
  CoC-уровни, pinch-нормы. → В хабе захардкожены, но **нет таблицы «ты vs норма
  твоего класса»**.
- **Bezkorovainyi ARM1 (патент #43082, WO2026106582A1):** Fmax/F100/F500/t05F/
  fatigue-тренд. → В хабе считается, но **нет популяционных норм** (что такое
  «хороший F/t»).
- **ACWR/тендоны:** per-muscle ACWR уже есть; **нет связки** «ACWR-danger точка →
  причина volume/fatigue в топе» (данные рядом, вывод не соединён).

## 2.1. Исследование PRO-2 — 6 свежих запросов (Sep 10 2026, этот раунд)

Старый план (`ARM-DIAGNOSTICS-PRO-PLAN.md`, Sep 05) исследовал движки
(~15 источников: Mithril/StrengthLog/GoldenGrip/Ezreal/Grokipedia/Bezkorovainyi/
IronMind/GripStrength/WAF). PRO-2 — НОВЫЙ док про подачу хаба; ниже — свежие
запросы именно под него (что проверено, что изменило эпики, что осталось открытым):

- **Техники (en.wikipedia.org/wiki/Arm_wrestling):** hook/toproll/press +
  rising/posting определения 1-в-1 с чипами хаба; press новичкам опасен →
  прямое обоснование **P5 press-гейта** (не выдумано).
- **WAF Rules 2025 (waf-armwrestling.com, PDF 18 стр.):** вес XXI века: L/R —
  отдельные зачёты (в хабе уже), фолы/ремень/сгонка. → Table-IQ покрывает
  фолы/срывы/ремень; правил-памятка «фол → чинить» — открытый пункт (не P1–P7).
- **RT WR (en.wikipedia.org/wiki/Rolling_Thunder_(exercise)):** Tyukalov 130.5
  (V3, 2013 Visegrip Viking); рекорды V1/V2 (Felix 146.7) — аннулированы как
  flawed units. → 130.5 в хабе корректен, причём именно V3; женщины —
  Gaiduchenko 77.2 (2012) 1-в-1. Обоснование **P3 norms-table**.
- **Apollon's Axle WR (ironmind.com, rules & records):** мужчины Myerscough
  **237.5** (2022), женщины Gaiduchenko 137.9. ⚠️ ОТКРЫТОЕ: в хабе Axle
  сравнивается с **133** (Saxon-тип; ср. Tyukalov Saxon 127.5, 2025) — для
  грифа Apollon это заниженный ориентир. Не менял кодом (тронуло бы нормы и
  тесты): честный follow-up — селект снаряда (Saxon/Apollon) в P3-таблицу.
- **CoC (en.wikipedia.org/wiki/Captains_of_Crush_Grippers + ironmind.com):**
  No.3 (~127 кг рейтинг) = мировой benchmark crush-грипа (288+ сертифицированных),
  No.4 — элита единиц (Myerscough 2023). ⚠️ Оговорка Strossen: рейтинг ≠
  калибровка — поэтому `cocLevel` в хабе остаётся ordinal-уровнем, а не кг.
- **Armlifting-дисциплины SAR (sportscategory.info + armliftingusa.com +
  ironmind.com):** RT / Axle / Hub / 2H Pinch Block / Excalibur / Silver Bullet
  / Saxon. → Хаб покрывает 5 из 7 (RT, Axle, Hub≈pinch, 2H-pinch, CoC≈Silver
  Bullet); Excalibur в хабе нет — открытый пункт.
- **ARM1 Bezkorovainyi (researchgate.net, Sep 2023 / Jun 2024):** FB5k-тензодинамометр,
  author block device, патент #43082, метрики F100/F500/t05F — прибор и метрики
  реальны. → Обоснование Strength-таба; популяционных норм F/t в паблике нет —
  поэтому в **P3** только качественный порог из кода (30), без выдуманных «элит».
- **Переломы (PMC9592113/PMC8667248/PMC10426487/Ogawa 2022/Kruczynski/Brismar):**
  9 из 10 серьёзных травм — спираль дистальной трети плеча; торсия+изгиб,
  пик 60 МПа на 115 мм выше локтя (medial-posterior); лучевой нерв 8–20%;
  пик возраста 20–24; подростки 14–15 — отрыв медиального надмыщелка (тяга
  flexor-pronator). → Обоснование **P5** (red-flags + side-капы + RIR≥2);
  открытый пункт: teen-гейт 14–15 лет (MHE-риск) — в P1–P7 не входил.
- **PMC11206825 (grip force–time review):** RFD считают наклоном кривой за
  10-мс окна, пик — первые 50–75 мс; единого стандарта нет. → F/t в хабе как
  ориентир ок, «эталона» не существует — честно отражено в P3-пороге.

## 3. План PRO (эпики P1–P7 — ВСЕ ВЫПОЛНЕНЫ, каждый своим коммитом pathspec)

- **P1 Навигация-мастер ✅ (`168424ab`):** HowTo-портянка → 4 строки
  (`1 Тело → 2 Точки → 3 Тесты → 4 План`); NEW `HubTabNext` («Шаг N из 5 ·
  Далее: … →», нейтральный aria-label — имена табов не двоятся, тест
  `arm-hub-tabpanel` «Вкладки, а не шаги» цел). Нумерацию самих пилюль НЕ
  вставили осознанно (a11y-контракт). DoD закрыт: новичок идёт по шагам.
- **P2 Дедуп панелей ✅ (`93b37085`):** `HubTableStrip` удалён (полоса живёт в
  `Pressure`); верхний CTA убран — остался ОДИН внизу (`HubAction`); Force
  Vector — только в `Grip` (в `Pressure` — refs-строка); scoring — только в
  Head; bio-карточки из `HubOutput` заменены одной строкой-ссылкой на
  `Wrist`; честный angle-бейдж side/back переехал к чипам выбора. DoD: −2
  AdCard, 0 дублей (поймано своим тестом «угол н/п», починено переносом, не
  откатом).
- **P3 Нормы-бенчмарки ✅ (`c2645ff5`):** `data-arm="norms-table"` (ad-kv
  строки RT vs WR %/Axle vs Saxon 133/Pinch vs 10с/Side-Back vs WAF-нормы —
  только заполненные поля); F/t-качество по существующему порогу 30
  (`⚠ низкая — чинить`/`✓ в допуске`, новых популяционных норм не выдумывали);
  ROM-нормы строкой у мобильности. DoD: каждая цифра имеет норму/дельту.
- **P4 Видео/VBT ✅ (`f1c7c9a7`):** тип траектории + «Что чинить: …» по типу
  (hook — пронация/pron_lock, toproll — rising, press — нейтраль + гейт);
  VBT-шкала `0 · warn · stop · факт` + volbar (`data-arm="vbt-scale"`).
- **P5 Безопасность ✅ (`9f0266c6`):** red-flags скрининг (5 чипов, 🔴 стоп +
  «→ К return-to-pull» через setTab, «скрининг, не диагноз»); press-гейт
  новичкам в `HubControls`. DoD: опасное состояние не пропустить.
- **P6 Сценарии ✅ (`066f7c79`):** NEW `HubScenarios` (`he_arm_diag_scenarios`,
  кап 6): снапшот полей + точек, «📥 Загрузить», «Δ vs сейчас» (RT/Side/
  Back/Pinch дельты + точки +/−). DoD: прогресс в 1 клик.
- **P7 Мост 2.0 ✅ (`daccba8f`):** превью «📦 Что уедет в конструктор»
  (точки/мышцы/динамика/дни/топ-1/гейты) + честный пустой стейт; по пути
  поймано: `dynKeys` считал null-метрики динамикой — фильтр truthy.

**Вне скоупа (осознанно):** железо/Hands-модель в бандл, годовой план из хаба,
ICS-экспорт, постановка диагнозов (только скрининг + «к врачу»).

## 4. Файлы

- `src/ui/screens/TrainingScreen_parts/arm-design.css` (скролл/компакт, хвост)
- `src/ui/screens/TrainingScreen_parts/arm-hub-panels.tsx` (HERO/GAP 2 константы)
- Этот план: `docs/ARM-DIAGNOSTICS-HUB-PRO-2.md` (NEW)
