# Планировщик питания — режимы + V2-скоринг: аудит и план (Aug 19 2026)

Сводный документ по результатам полного критического анализа и тестирования всех режимов
планировщика питания и действующего V2-скоринга. Реализованные исправления отражены в коде,
план доработки и перечень новых функций — ниже для последующих раундов.

## 1. Режимы планировщика: pro / simple / minimal

Реализовано и проверено (эта область):
- `PlannerMode = 'pro' | 'simple' | 'minimal'` (types.ts), persist `he_planner_mode`.
- `quality: 'full' | 'basic'` в `MealPlanInput`; модульный `_qualityMode` в meal-plan-engine
  (синхронная генерация, сброс в finally).
- Pro: полный V2-функционал. Simple: без quality-weighted pick, бюджетного фильтра по
  bb_quality_score, микроанализа (coverage: []), MPS/micro-заметок, DIAAS/GL/II и чипа
  «Pro движок», mpsCheck, healthScore=null, calcMealScoreV2/runCalc, NutritionQualityCard,
  отчёта «Качество», рекомендаций V2, продвинутого/таргетинг-компоновщика.
- Minimal: ранний компактный рендер Settings-вкладки (вес/рост/возраст/пол/цель/приёмы +
  КБЖУ-карточка).
- Обходной фикс мемоизации: `renderMealList = useRenderMealList({ ...ctx, plannerMode })`,
  `finalCtx = useMemo(..., [ctx, plannerMode, renderMealList])`.

Тестирование: planner-область 229/229 (196 + 29 usefulness-calc + 4 nutrition-v2-audit),
tsc 0, vite build OK.

## 2. Исправлено в ходе аудита (закоммичено)

- `product-usefulness-v2.engine.ts`: `safeWeight()` — фильтр отрицательных/NaN весов в
  calcMealScoreV2/calcMealDIAAS/analyzeDailyDiet; fallback лейцина `protein × 75` в scorer;
  (ранее) insulinRicohet — только intra_workout.
- `nutrition-v2.engine.ts`: `'cut'` (маппинг fat_loss/cutting) теперь дефицит во всех ветках
  (goalMult 0.85, белок 2.5 г/кг LBM, карб-флор ×0.7); тип goal расширен
  (strength/recomp/rehab); удалён дубль-пересчёт carbsG, который затирал тренировочный
  карб-флор.
- Тесты: обновлён totalW-guard (веса 0 → скор 0), добавлены тесты негативных/NaN весов,
  `nutrition-v2-audit.test.ts` (4 теста: cut=deficit, порядок kcal, тренировочный карб-флор,
  лимит бюджета).

## 2a. Режимы — доводка по жалобам (Aug 19, закоммичено)

Жалобы: «простой режим — вкладки не скрыты, очень много информации, режим не работает»;
«быстрый режим — оставить три цели (масса/сушка/поддержание) и разрешить ручной ввод КБЖУ».

- **index.tsx — вкладки по режиму** (`isTabAllowed`): pro — все 6; simple — Настройки/План/
  Компоновщик (скрыты Отчёт, Нагрузка БЖУ, Тапер ББ); minimal — только Настройки. Если
  активная вкладка скрыта — рендер Настроек (fallback).
- **simple — карточки настроек**: продвинутые скрыты (`plannerMode === 'pro'`): Диетические
  паузы, Фаза и препараты, Скользящие графики, Уровень бюджета, Разнообразие, Уровень
  питания, 💼 Работа, 🌙 Вечерний режим, 🏭 Подбор продуктов, ⚡ Быстрые пресеты,
  Циклирование, ➕ Спецприём (Тапер/Электролиты/v2 Скоринг уже были pro-гейты). Остаются
  базовые: Пользователь, Цель, Привязка к тренировке, КБЖУ, Расписание, Аллергены,
  Здоровье, Тип плана, Предпочтения.
- **minimal — 3 цели**: `MINIMAL_GOALS = mass/cutting/maintenance`; при входе в быстрый
  режим цель нормализуется в maintenance, если вне списка (контекст, useEffect).
- **minimal — ручное КБЖУ**: тоггл «✏️ Ручное КБЖУ» → PopupNumber ккал/Б/Ж/У через
  kbjuMode/manual* (введённые значения учитываются в effective* и сборке рациона).
- Тесты: NEW `planner-mode-tabs.test.tsx` (7): вкладки pro/simple/minimal, скрытие Отчёта
  при переключении, simple-карточки, 3 цели, ручное КБЖУ. IndividualPlan 203/203, tsc 0.
  Файлы другого агента (RED-S/железо, коммиты 937f45bf0/76ef79aca «БЕЗ PUSH») не тронуты.

## 3. План доработки действующего V2-функционала

### P0 — целостность данных
1. **Единый DIAAS-модуль**: ✅ выполнено. `kbju-food-match.engine` теперь использует
   канонический `calcDIAAS` из V2: одинаковые reference/digestibility, оценка Tyr, cap 1.5
   и `reliable`; limiting AA переводится в русскую подпись для UI. Добавлен тест паритета
   «DIAAS компоновщика = DIAAS V2».
2. **Дефляция meal-DIAAS**: ✅ выполнено. В `calcMealDIAAS` белок продуктов БЕЗ
   амино-профиля больше не идёт в знаменатель (аминокислоты 0) — знаменатель и digest
   учитывают только пищу с профилем; `reliable=false`/'неполный' сохраняется. Добавлен
   тест «пища без профиля не занижает DIAAS приёма».
3. **Распространить safeWeight на входы MealComposer** (граммы из UI-чипов).
   ✅ Выполнено в `MealQuickControls.doUpdateWeight`: граммовка клампится в диапазон
   10-2000 г и отфильтровывает NaN/0/отрицательные на границе UI (зеркалит safeWeight
   движка) — макросы приёма не обнуляются/не инвертируются.

### P1 — корректность физиологии
4. **Профильные микро-таргеты** в analyzeDailyDiet/nutrition-quality: сейчас статичные
   (Zn 8, Mg 300, K 3500, VitC 50, Se 50, B2 1, B3 10, цистеин 500). → пол/возраст/фаза;
   женский набор (Fe 18, Ca 1000, D), PED-набор (K/Na/Mg/Cr), prep-набор.
5. **Скор дня**: analyzeDailyDiet не агрегирует скоры приёмов → UI-прогресс качества дня.
6. **Пробел в тестах**: гистамин, антинутриенты, глютатион, электролиты, ammoniaRisk,
   HOMA-IR — matrix-тесты на каждый флаг.
7. **Дублирование DIGEST/DIET_PROFILE** (разные значения veg_fruit 0.78 vs 0.85) — одна константа.

### P2 — UX/консистентность
8. Отчёт дня в печати (скор + флаги + покрытие).
9. Семантика `cortisolRisk` («недостаточно быстрых углеводов post-workout») — пояснение в UI.
10. HOMA-IR в карточке дня.

## 4. Дополнительные функции V2 (новые)

1. Скор дня (1-10) + тренд 7/30 дней, бейдж «качество дня».
2. Покрытие микронутриентов (% от профильных таргетов, ~12 ключевых) + «закрыть дефицит» в компоновщике.
3. Протеин-тайминг: равномерность белка по приёмам — диапазон НЕ фиксированный,
   а по сухой массе (FFM): 0.4-0.55 г/кг FFM на приём (кап ~50 г), минимум лейцин 2.5-3 г/приём;
   25-40 г — лишь ориентир для среднего атлета (70-80 кг). Вердикт «MPS-окна».
4. Окна тренировки (pre/intra/post): скоринг окна с учётом времени приёма.
5. **Peri-workout в дневнике — с учётом интенсивности и PED**: ✅ выполнено в
   `nutrition-periworkout.engine.ts` и `PeriWorkoutCard.tsx`. Объём/состав pre/intra/post
   масштабируются по интенсивности (low/medium/high; авто по avg RPE/длительности), цель
   нормализуется для `fat_loss/cutting`, а PED-контекст (инсулин/ГР/ИФР-1 и дозовые флаги)
   даёт безопасное предупреждение без назначения дозировок и без автоматического добавления
   углеводов. Добавлены 3 теста: интенсивность, alias цели, PED safety warning.
6. Омега-индекс: абсолютные DHA+EPA (≥250 мг/день), не только соотношение.
6. Интеграция с contest prep: скоринг приёмов по фазам (вода/натрий/карб-моды).
7. Женский модуль: железо/кальций/фаза цикла в дефицитах и подсказках.
8. Вечерний приём vs сон: триптофан/магний/глицин вечером.
9. Variety-скор дня (разнообразие групп продуктов).
10. Гидратация в питании: вода/натрий/калий в дневном отчёте.
11. Сравнение день vs день, детект ухудшения качества на диете.
12. Экспорт дневного/недельного отчёта питания (PDF) со скором и флагами.
