# МЕДИЦИНСКИЙ АУДИТ — BioStackAIScreen + BB-builder

**Аудитор:** Chief Physician (модель glm-5.2)
**Дата:** 29 Jul 2026
**Гайдлайны:** Endocrine Society 2022, AACE/ACE 2023, EAU 2023, ESC 2023, AASLD 2021, ASA 2021, Endocrine Society hCG/AI guidelines

**Аудитируемые файлы:**
- D:\BodyBuildHealth\src\engines\recommendation-engine.ts (959 строк)
- D:\BodyBuildHealth\src\data\lab-priority-map.ts (1095 строк)
- D:\BodyBuildHealth\src\engines\support-plan\engine-helpers.ts (945+ строк)
- D:\BodyBuildHealth\src\data\mechanism-support-bridge.ts (201 строка)
- D:\BodyBuildHealth\src\engines\support-plan\shared-constants.ts (270 строк)
- D:\BodyBuildHealth\src\core\constants.ts (UCUM_MAP, normalizeLabValue)

---

## РЕЗЮМЕ ДЛЯ РУКОВОДИТЕЛЯ (EXECUTIVE SUMMARY)

### Критичность находок: ВЫСОКАЯ

Движок имеет **значительные медицинские расхождения** с актуальными клиническими гайдлайнами 2021+. Основные проблемы:

1. **КРИТИЧНО:** Пороги назначения AI и cabergoline не соответствуют рекомендациям Endocrine Society (строки 266-293, 297-316).
2. **КРИТИЧНО:** hCG-протокол 500 МЕ 2×/нед — противоречит современным данным (строки 319-327, 390-395).
3. **КРИТИЧНО:** Тромбофилия обрабатывается БАДами вместо антикоагулянтов (строка 670).
4. **ВЫСОКО:** Нормализация E2/Prolactin имеет эвристические пороги с риском ложной конвертации (constants.ts:582-630).
5. **ВЫСОКО:** Пропущены ключевые противопоказания для cabergoline, niacin, красного риса.
6. **СРЕДНЕ:** Severity-фильтрация через `deriveSeverity` работает, но пороги (×2, ×3) произвольны, не клинически валидированы.

**Рекомендация:** Движок НЕ готов для автономных медицинских назначений без верификации врачом. Требуется коррекция порогов и добавление недостающих противопоказаний.

---
---

## 1. ПРАВИЛА НАЗНАЧЕНИЯ ВЕЩЕСТВ ПРИ ОТКЛОНЕНИЯХ АНАЛИЗОВ

### 1.1 Архитектура назначения (3 фазы)

Движок `evaluateRecommendations` (recommendation-engine.ts:113-734) работает в 4 фазы:

**Фаза 1 (строки 164-262):** Сканирование каждого ААС в курсе → pd-effects (pharmacodynamic) → запрос support-index по механизмам.
- `maxHct`, `maxHep`, `maxNeuro`, `maxAro`, `maxProg`, `maxLipid` агрегируются как максимум по всем активным препаратам (строки 167-184).
- Триггеры: `pd.hct_impact >= 3`, `pd.hepatotoxicity > 0`, `pd.neuro_toxicity >= 0.3`, `pd.aromatization > 0`, `pd.progestogenic > 0`, `pd.lipid_impact > 0.2`.

**Фаза 3 (строки 397-559):** Лабораторные правила — массив `labRules` (строки 497-521) с порогами `threshold.value` и направлением `higherIsWorse`. Триггер: `val > threshold.value` или `val < threshold.value` (строка 529).

**Фаза 4 (строки 621-714):** User state → рецепты `stateRecipes` (массив из ~40 условий, строки 625-700).

### 1.2 Триггеры и пороги — детальная таблица

| Маркёр | Порог срабатывания | Источник | severe-порог | Клин. референс 2021+ | Соответствие |
|--------|--------------------|---------|--------------|----------------------|--------------|
| HCT | >47% (муж), >44% (жен) | rec-eng:203 | >52% | ESC/ASA: эритроцитоз >52% (муж), >48% (жен) | ⚠️ Порог 47% слишком низкий — это нормальная верхняя граница. Назначает фибринолитики при нормальном HCT. |
| ALT | >40 U/L | rec-eng:225, labRules:498 | >100 U/L | AASLD: ULN 33 (муж)/25 (жен) для MAFLD; 40 — старый стандарт | ⚠️ Порог 40 устарел. AASLD 2021 рекомендует 30/19. Низкая чувствительность. |
| AST | >40 U/L | labRules:499 | — | AASLD: ULN 32 (муж)/25 (жен) | ⚠️ Аналогично ALT. |
| GGT | >55 U/L | labRules:500 | — | EASL: ULN 40 (муж)/30 (жен) | ⚠️ 55 — завышен, пропускает ранний холестаз. |
| Bilirubin | >21 µmol/L | labRules:501 | — | Реал-референс 21 — корректен | ✅ OK |
| Creatinine | >105 µmol/L | labRules:502 | — | KDIGO: ULN 106 (муж)/97 (жен) | ✅ OK (погранично) |
| Urea | >8 mmol/L | labRules:503 | — | Реал: ULN 7.1-8.3 | ✅ OK |
| Glucose | >5.6 mmol/L | labRules:504 | — | ADA 2023: предиабет ≥5.6 (IFG) | ✅ OK, соответствует ADA |
| Homocysteine | >15 µmol/L | labRules:505, rec-eng:467 | >30 | AHA: гипергомоцистеинемия >15; тяжёлая >30 | ✅ OK |
| LDL | >3.0 mmol/L | labRules:506 | >4.5 | ESC 2019/2023: цель <2.6 (высокий риск), <1.8 (оч. высокий) | ⚠️ Порог 3.0 — устарел. ESC 2019 рекомендует лечение при >2.6 для ААС-пользователей (высокий риск). |
| Triglycerides | >1.7 mmol/L | labRules:507 | — | AACE 2023: гипертриглицеридемия ≥1.7 | ✅ OK |
| HDL | <1.0 mmol/L | labRules:508 | — | ESC: <1.0 (муж)/<1.2 (жен) | ⚠️ Нет половой дифференцировки |
| CRP | >5 mg/L | labRules:509, rec-eng:483 | >20 | hs-CRP: <1 низкий, 1-3 средний, >3 высокий; >10 острое воспаление | ⚠️ Порог 5 — для обычного CRP, но при hs-CRP >3 уже патология. Нет различения hs-CRP vs CRP. |
| Cortisol | >550 nmol/L | labRules:510 | — | Endocrine Society: гиперкортизолемия >550 (утро) — корректно, НО требует тестов подавления | ⚠️ Триггер без динамического теста = ложноположительный. |
| D-dimer | >0.5 µg/mL | labRules:511 | — | ASC 2021: ULN зависит от возраста (<50: 0.5; >50: возраст×0.01) | ⚠️ Нет возрастной корректировки Fibrinogen D-dimer. У пожилых будет ложная тревога. |
| Fibrinogen | >4 g/L | labRules:512 | — | ESC: гиперфибриногенемия >3.5-4.0 | ✅ OK |
| Ferritin | >300 µg/L | labRules:513, rec-eng:435 | >500 | Муж: >300 избыток; >500 — гемохроматоз скрининг | ✅ OK, но без половой дифференцировки (жен: >150) |
| Vitamin D | <30 ng/mL | labRules:514, rec-eng:403 | <15 | Endocrine Society 2021: дефицит <20, недостаточность 21-29 | ⚠️ Порог 30 — слишком агрессивный. По ES 2021: <30 = недостаточность (не дефицит), но движок маркирует как «дефицит» (rec-eng:410). |
| B12 | <210 pg/mL | labRules:515, rec-eng:419 | <100 | WHO/British: дефицит <200 (пограничный 200-300); неврологические симптомы требуют лечения при <300 | ⚠️ 210 — погранично, пропускает функциональный дефицит (особенно при высоком гомоцистеине). |
| TSH | >4.5 mIU/L | labRules:516 | — | ATA 2023: гипотиреоз >4.5; субклинический 4.5-10 | ✅ OK, но нет различения субклинического vs манифестного |
| E2 | >40 pg/mL | rec-eng:266, labRules:517 | >55 | Endocrine Society: для мужчин на TRT цель E2 <40 pg/mL — корректно | ✅ OK (но см. проблему нормализации ниже) |
| Prolactin | >25 ng/mL | rec-eng:297, labRules:518 | >40 | Endocrine Society: гиперпролактинемия >25 (муж); макропролактинома >200 | ✅ OK, но нет проверки на макропролактинемию |
| PSA | >4 ng/mL | labRules:519 | — | AUA 2023: ≥4 — биопсия, НО возрастные пороги (50-59: 2.5; 60-69: 3.0; 70-79: 4.5) | ⚠️ Нет возрастной корректировки PSA. |
| DHEA-S | <100 µg/dL (муж) | labRules:520, rec-eng:451 | <50 | Endocrine Society: возрастные нормы, 100 — нижняя граница для 40-49 лет | ⚠️ Нет возрастной корректировки. У 20-30 лет норма 200-500, у 60+ — 50-200. |

### 1.3 Правила выбора веществ (lab-priority-map)

Для каждого маркёра — упорядоченный список (`lab-priority-map.ts:31-900`):
- `priority: 1|2|3|4` — 1-й/2-й/3-й/4-й выбор
- `minSeverity: 'mild'|'moderate'|'severe'` — при какой степени отклонения назначать
- `getPrioritySubstances(marker, severity)` (строка 993) фильтрует по `order.indexOf(e.minSeverity) <= sevIdx` (строка 1000)

**Логика severity-фильтрации (getPrioritySubstances):**
```
mild    → только priority с minSeverity='mild'
moderate → minSeverity in ['mild','moderate']
severe  → minSeverity in ['mild','moderate','severe']
```
Это **корректный каскад**: при утяжелении состояния подключаются более сильные препараты.

**Проблема:** Фильтр `>=2` в rec-eng (например, строки 207, 228, 268, 300, 351, 370, 405, 421, 437, 453, 469, 485) требует минимум 2 вещества из priority-map, иначе fallback на механизм-based поиск. Это создаёт несоответствие: при severe может быть назначен только 1 приоритетный + fallback, а не полный каскад.

---

## 2. SEVERITY УРОВНИ И ФИЛЬТРАЦИЯ

### 2.1 Типы severity

**В lab-priority-map.ts:12:** `SeverityLevel = 'mild' | 'moderate' | 'severe'` — для порогов назначения веществ.

**В recommendation-engine.ts:10:** `RecSeverity = 'critical' | 'high' | 'medium' | 'low' | 'info'` — для приоритета рекомендаций (не веществ).

**Конфликт:** Две разные шкалы severity. `deriveSeverity` (lab-priority-map.ts:1081-1095) переводит значение → mild/moderate/severe, но `addRec` (rec-eng:143) использует critical/high/medium/low/info. Маппинг между ними — ручной в каждой фазе (например, rec-eng:204-205, 226-227, 267, 298).

### 2.2 deriveSeverity — ЛОГИКА (lab-priority-map.ts:1081-1095)

```typescript
if (higherIsWorse) {
  ratio = value / defaultValue;
  if (ratio >= 3.0) return 'severe';
  if (ratio >= 2.0) return 'moderate';
  if (ratio >= 1.2) return 'mild';
}
```

**Проблема:** Пороги ×2 и ×3 — произвольные, не клинически валидированы.
- ALT 40 → severe при >120 (×3). AASLD 2021: тяжёлая гепатотоксичность — ALT >8×ULN (>320) или билирубин >2×ULN (Hy's law). Движок маркирует «severe» при 120, что не соответствует Hy's law.
- HCT 52% → severe при >156% (×3) — невозможно, т.к. HCT >70% несовместимо с жизнью. На практике severe-порог HCT задан жёстко >52% (rec-eng:204), а не через deriveSeverity.
- Creatinine 105 → severe при >315. KDIGO: AKI stage 3 = ×3 от baseline. Корректно, но 315 µmol/L — это уже терминальная почечная недостаточность.

**Несоответствие:** `deriveSeverity` вызывается только в Phase 3 (labRules, rec-eng:533), а Phase 1 (rec-eng:204, 226, 267, 298) использует свои жёстко заданные пороги. Это создаёт две разные системы оценки — несогласованность.

### 2.3 Фильтрация в addRec (rec-eng:143-162)

Фильтрация веществ:
1. `blockedIds.includes(sid)` — пользовательские негативные реакции (строка 146)
2. `REC_BLACKLIST.has(sid)` — авто-блэклист (строка 147, shared-constants.ts:171-193)
3. `usedSubstanceIds.has(sid)` — дедупликация (строка 148)
4. `isClassBlocked(sid)` — same-class дедупликация (строка 149)

**Проблема:** Severity не влияет на фильтрацию внутри `addRec`. Все severity проходят одинаковый фильтр. Сортировка только по `orderByPriority` (rec-eng:134-141). Для critical-рекомендаций нет усиленной проверки — они могут быть заблокированы тем же блэклистом, что и low.

### 2.4 Фильтрация по coverage level (applyCoverageLevel, rec-eng:737-808)

`filterByCoverageLevel` (строка 746) фильтрует по уровню (basic/mid/max/boost). Логика:
- basic: удаляются рецептурные (telmisartan, nebivolol, l_dopa, gaba, bile_acids, artichoke, bergamot, red_yeast, probiotics — engine-helpers.ts:604)
- mid: удаляются telmisartan, nebivolol (engine-helpers.ts:607)

**Проблема:** При basic уровне **удаляются антигипертензивные** (telmisartan, nebivolol). Если у пользователя АД >160/100 (hypertension2, critical), при basic уровне он не получит NOTHING для АД. Это **медицински опасно** — critical-рекомендации должны bypass coverage-фильтр.

### 2.5 Лимит 3 вещества на рекомендацию (rec-eng:797)

`const finalIds = keep.slice(0, 3)` — максимум 3 вещества на рекомендацию.

**Проблема:** При severe-степени гепатотоксичности (ALT >100) clinically нужно 4+ (NAC + TUDCA + silymarin + ALA + фосфолипиды), но движок ограничит 3.

---

## 3. ТОЧНОСТЬ ЛАБОРАТОРНЫХ ПОРОГОВ vs КЛИНИЧЕСКИЕ РЕФЕРЕНСЫ 2021+

### 3.1 Нормализация единиц (constants.ts:582-630) — КРИТИЧНАЯ ПРОБЛЕМА

`normalizeLabValue` конвертирует РФ-единицы в стандартные. Эвристика по величине значения:

**E2 (constants.ts:585-591):**
```
if (rawValue > 50) → rawValue / 3.67  // считаем pmol/L → pg/mL
```
**Проблема:** Если мужчина на TRT имеет E2 = 60 pg/mL (что бывает при высокой ароматизации без AI), движок посчитает это pmol/L и конвертирует → 16.3 pg/mL. В результате **отменится назначение AI при реально высоком E2**. Это **медицински опасная ошибка** — риск гинекомастии.

**Prolactin (constants.ts:593-599):**
```
if (rawValue > 100) → rawValue / 21.2  // mIU/L → ng/mL
```
**Проблема:** Менее опасна (ng/mL >100 = крайне редко), но при макропролактинемии (big prolactin) значения могут быть >100 ng/mL — будет ложная конвертация.

**Total T (constants.ts:601-607):**
```
if (rawValue > 100) → rawValue * 0.0347  // ng/dL → nmol/L
```
**OK** — nmol/L редко >100 (норма 12-33), ng/dL всегда >100. Эвристика работает.

**Cortisol (constants.ts:609-614):**
```
if (rawValue < 50) → rawValue * 27.59  // μg/dL → nmol/L
```
**Проблема:** nmol/L <50 = гипокортизолизм (Addisonian crisis). Если у пациента реальный nmol/L = 40 (тяжёлая надпочечниковая недостаточность), движок конвертирует → 1103 nmol/L = ложный гиперкортизолизм. **Опасная ошибка** — пропустит adrenal insufficiency.

**DHEA-S (constants.ts:616-621):**
```
if (rawValue < 20) → rawValue * 36.9  // μmol/L → μg/dL
```
**Проблема:** μg/dL <20 = надпочечниковая недостаточность (Addison). Движок конвертирует → 738 μg/dL = норма. **Опасная ошибка**.

### 3.2 Сверка порогов с гайдлайнами

| Маркёр | Порог в коде | Гайдлайн 2021+ | Соответствие | Файл:строка |
|--------|--------------|----------------|--------------|-------------|
| ALT | 40 | AASLD 2021: 30 (M)/25 (F) | ❌ Устарел, пропускает MAFLD | rec-eng:498 |
| AST | 40 | AASLD 2021: 32 (M)/25 (F) | ❌ Устарел | rec-eng:499 |
| GGT | 55 | EASL: 40 (M)/30 (F) | ❌ Завышен | rec-eng:500 |
| LDL | 3.0 | ESC 2019/2023: 2.6 (высокий риск), 1.8 (оч. высокий) | ❌ Устарел для ААС-пользователей | rec-eng:506 |
| HDL | 1.0 (оба пола) | ESC: 1.0 (M)/1.2 (F) | ⚠️ Нет половой дифференцировки | rec-eng:508 |
| HCT | 47 (M)/44 (F) | ESC/ASA: 52 (M)/48 (F) для эритроцитоза | ❌ Слишком низкий, гипердиагностика | rec-eng:203 |
| HCT severe | 52 | ESC: >54 — терапевтическая флеботомия | ⚠️ 52 — погранично, но допустимо | rec-eng:204 |
| Vitamin D | 30 | Endocrine Society 2021: 20 (дефицит), 30 (недостаточность) | ⚠️ Маркировка «дефицит» при 30 — некорректна | rec-eng:403,410 |
| B12 | 210 | BSH 2019: 200 (дефицит), 200-300 (пограничный) | ⚠️ Пропускает функциональный дефицит | rec-eng:419 |
| TSH | 4.5 | ATA 2023: 4.5 (гипотиреоз), но субклинический требует повторного теста | ✅ OK | rec-eng:516 |
| E2 | 40 | Endocrine Society TRT: <40 цель | ✅ OK (но проблема нормализации) | rec-eng:266 |
| Prolactin | 25 | Endocrine Society: >25 (M) гиперпролактинемия | ✅ OK | rec-eng:297 |
| PSA | 4 (оба возраста) | AUA 2023: возрастные пороги | ❌ Нет возрастной корректировки | rec-eng:519 |
| DHEA-S | 100 (оба возраста) | Endocrine Society: возрастные нормы | ❌ Нет возрастной корректировки | rec-eng:451 |
| Ferritin | 300 (оба пола) | Муж: >300, Жен: >150 (premeno), >200 (post) | ⚠️ Нет половой дифференцировки | rec-eng:435 |
| D-dimer | 0.5 (оба возраста) | ASC 2021: возрастной порог (age×0.01 для >50) | ❌ Нет возрастной корректировки | rec-eng:511 |
| CRP | 5 | AHA: hs-CRP >3 (высокий риск); >10 (острое) | ⚠️ Порог 5 — не различает hs-CRP vs обычный | rec-eng:483 |
| HbA1c | (в UCUM) 5.7% | ADA 2023: предиабет 5.7-6.4%, диабет ≥6.5% | ✅ OK | constants.ts:433 |
| HOMA-IR | 2.7 | ATPIII: >2.5 IR; AACE: >2.0 | ✅ OK | constants.ts:436 |
| Creatinine | 105 | KDIGO: 106 (M)/97 (F) | ✅ OK | rec-eng:502 |

### 3.3 Гематокрит — двойной стандарт

- `DYNAMIC_REFS.HCT.baseULN = 52` (constants.ts:402) — корректно для мужчин
- `rec-eng:203` — триггер при >47 (M)/>44 (F) — **это LLN, не ULN**. Назначает фибринолитики при нормальном HCT.
- `UCUM_MAP.HCT.uln = 52` (constants.ts:423) — корректно

**Вывод:** Движок использует LLN+5% как триггер, что создаёт ложные рекомендации. Должно быть: триггер при >50% (M), severe при >52%, флеботомия при >54%.

---

## 4. ЛОГИКА MANDATORY НАЗНАЧЕНИЙ — МЕДИЦИНСКАЯ ОБОСНОВАННОСТЬ

### 4.1 hCG — автоназначение (rec-eng:319-327, 390-395)

**Логика:** Любой ААС → hCG 500 МЕ 2×/нед, схема 3/1 (3 нед приём, 1 нед отдых).

**Гайдлайны:**
- Endocrine Society TRT guidelines (Bhasin 2018): hCG при TRT для сохранения фертильности — 500-3000 МЕ 2-3×/нед. Андрология 2022 meta-analysis: hCG эффективен для предотвращения атрофии яичек.
- **Схема 3/1** — не подтверждена в RCT. Стандарт — непрерывный приём на курсе.
- **Дозировка 500 МЕ 2×/нед** — в нижнем диапазоне (500-3000). Для ААС-курсов с высокими дозами (>500 мг/нед тестостерона) 500 МЕ может быть недостаточно. RCT (Wenker 2015): 250-500 МЕ 2×/нед достаточно для большинства.

**Проблемы:**
1. **Нет дифференциации по дозе ААС.** При 1000 мг/нед тестостерона нужно больше hCG, чем при 250 мг/нед. Движок назначает одинаковую дозу.
2. **Схема 3/1 не валидирована.** Endocrine Society не рекомендует циклирование hCG.
3. **Нет проверки на противопоказания:** hCG может усиливать ароматизацию → ↑E2. Движок не предупреждает, что hCG может потребовать увеличения AI.
4. **Нет проверки на гипогонадизм в анамнезе** — при вторичном гипогонадизме hCG менее эффективен.

**Оценка:** Триггер (любой ААС → hCG) — обоснован. Дозировка в нижнем диапазоне — допустима. Схема 3/1 — **необоснована**, требует RCT или исключения. Нет адаптации по дозе ААС — упущение.

### 4.2 Anastrozole (AI) — триггер (rec-eng:266-293, 330-336)

**Логика Phase 1:**
- `maxAro > 0` (любая ароматизация) ИЛИ `pastGyno` ИЛИ `E2 > 40` → рекомендация
- При `maxAro >= 0.5` ИЛИ `E2 > 55` → добавляется anastrozole (строка 273-278)

**Note (rec-eng:330-336):** Если AI ещё не назначен — note «доза по результатам анализов».

**Гайдлайны:**
- Endocrine Society TRT: AI не рутинно, только при симптомах гинекомастии или E2 >40 pg/mL (мужчины).
- **Порог E2 >40 для триггера** — корректен.
- **Порог E2 >55 для anastrozole** — произвольный. Нет RCT для этого порога. Часть эндокринологов использует >50, другие — симптоматический подход.

**Проблемы:**
1. **Профилактический AI при pastGyno** — обоснован (высокий риск рецидива).
2. **Профилактический AI при maxAro > 0** — **преждевременное назначение**. Должно быть: триггер по **лабораторному E2**, а не по предсказанной ароматизации. Назначение AI «на всякий случай» = риск краша E2 (депрессия, либидо, кости).
3. **Нет предупреждения о краше E2.** При AI на ПКТ (PHASE_BLOCKLIST.pct, shared-constants.ts:217-219) блокируется — корректно. Но в Phase 1 нет проверки фазы.
4. **Дозировка anastrozole 1 мг 2×/нед** (rec-eng:290) — стандартная стартовая, но без адаптации по дозе тестостерона. `applyTitration` (engine-helpers.ts:628-632) адаптирует: totalTest >700 → 1.5 мг, >500 → 1 мг, иначе 0.5 мг. **Корректно.**
5. **Нет проверки на E2 < 10 pg/mL** (краш) — нет обратной рекомендации прекратить AI.

**Оценка:** Триггер по E2 >40 — обоснован. Триггер по `maxAro > 0` без лабораторного подтверждения — **преждевременный**. Нет мониторинга краша E2 — упущение.

### 4.3 Cabergoline — триггер (rec-eng:297-316, 338-345)

**Логика:**
- `maxProg > 0` ИЛИ `has19Nor` ИЛИ `Prolactin > 25` → рекомендация
- При `maxProg >= 0.4` ИЛИ `has19Nor` → добавляется cabergoline (строка 304-309)

**Гайдлайны:**
- Endocrine Society hyperprolactinemia: cabergoline при симптомной гиперпролактинемии. Бессимптомная макропролактинемия — наблюдение.
- **Триггер Prolactin >25** — корректен для мужчин (ULN 15-20, гиперпролактинемия >25).
- **Триггер по 19-nor (tren/nandrolone)** — **проблема.** 19-nor сами по себе не повышают пролактин напрямую. Механизм: progestogenic activity → ↑E2 (через ароматизацию) → ↑SHBG → ↑пролактин. Но это опосредованный путь. Назначение cabergoline **превентивно** при 19-nor **без подтверждённого ↑пролактина** — не рекомендуется Endocrine Society.

**Проблемы:**
1. **Превентивный cabergoline при 19-nor** — не обоснован без лабораторного подтверждения. Риск: кардиальная вальвулопатия (cabergoline >3 мг/нед, редкие случаи), импульсная компульсивность (дофаминовая дисрегуляция).
2. **Дозировка cabergoline 0.25 мг** (rec-eng:313, engine-helpers.ts:634) — низкая, безопасная. Но при Prolactin >40 (severe) нужна титрация до 0.5 мг, чего нет в коде.
3. **Нет проверки на противопоказания:**
   - **Фиброз сердечных клапанов** (cabergoline-индуцированная вальвулопатия — FDA warning)
   - **Психоз/шизофрения** (дофаминовый агонист может усилить)
   - **Артериальная гипотензия** (cabergoline снижает АД)
   - **Беременность** (категория B, но избегать)
4. **Нет проверки на макропролактин** — при макропролактинемии (big prolactin) уровень высокий, но клинически неактивен. Cabergoline не нужен.

**Оценка:** Триггер по Prolactin >25 — обоснован. Триггер по 19-nor без лаб. подтверждения — **преждевременный**. Нет противопоказаний (вальвулопатия, психоз) — **опасное упущение**.

### 4.4 TUDCA — триггер (rec-eng:223-245, lab-priority-map:36)

**Логика:**
- `maxHep > 0` ИЛИ `hasOral` ИЛИ `altAstElevation != none` ИЛИ `ALT > 40` → рекомендация
- TUDCA — priority 2, minSeverity='moderate' (lab-priority-map.ts:36)

**Гайдлайны:**
- AASLD 2021: UDCA (TUDCA) — препарат выбора для холестаза (PBC, PSC). Для AAS-индуцированной гепатотоксичности — ограниченные данные.
- **Триггер ALT >40** — устаревший порог (AASLD 2021: 30/25).
- **TUDCA при hasOral** — обосновано (17α-алкилированные ААС вызывают холестаз + цитолиз).
- **Дозировка 500-1000 мг** (engine-helpers.ts:638) — стандартная (UDCA 13-15 мг/кг/день = ~1000 мг для 70 кг). Корректно.

**Проблемы:**
1. **Нет проверки на полный холестаз** (GGT + ALP + прямой билирубин). TUDCA менее эффективен при чистом цитолизе без холестаза.
2. **Нет предупреждения:** TUDCA может снижать всасывание других препаратов (связывание жёлчных кислот).

**Оценка:** Триггер обоснован, порог ALT устарел. Дозировка корректна.

### 4.5 NAC — триггер (rec-eng:223-245, lab-priority-map:35)

**Логика:** Аналогично TUDCA. NAC — priority 1, minSeverity='mild' (lab-priority-map.ts:35).

**Гайдлайны:**
- AASLD 2021: NAC — препарат выбора при остром парацетамоловом гепатите. Для AAS-индуцированной — ограниченные данные, но используется off-label.
- **Дозировка 1200-1800 мг** (engine-helpers.ts:637) — стандартная. При острой гепатотоксичности — до 2400 мг (rec-eng:242).
- **Схема нарастания (engine-helpers.ts:637):** 1200 мг (нед 1-2) → 1800 мг. Корректно для избежания GI-побочки.

**Оценка:** Обосновано. Дозировка корректна.

### 4.6 Aspirin — триггер (lab-priority-map:138, 146)

**Логика:** HCT↑, PLT↑, D-dimer↑, MPV↑ → aspirin (priority 1-3).

**Гайдлайны:**
- ASA 2021: первичная профилактика aspirin при возрасте 40-70 + риск ≥10%. Не для всех.
- **Проблема:** Назначение aspirin при HCT↑ без учёта кровотечений, язвы, возраста. **Опасно для <40 лет с низким CV-риском.**
- **Нет проверки на:** язву ЖКТ, геморрагический диатез, возраст <40 (не рекомендуется первичная профилактика).

**Оценка:** Триггер по HCT/PLT — умеренно обоснован, но **нет противопоказаний** — опасно.

---

## 5. ПРОПУЩЕННЫЕ ПРОТИВОПОКАЗАНИЯ И ОПАСНЫЕ КОМБИНАЦИИ

### 5.1 getContraindicationAlerts (engine-helpers.ts:806-815) — НЕПОЛНЫЙ

Текущие алерты:
- `hasCVD` → избегать стимуляторов
- `hasThrombophilia` → контролировать HCT
- `hasLiverDisease` → минимизировать оральные ААС
- `hasKidneyDisease` → избегать НПВС
- `allergies` → показать

**Пропущенные КРИТИЧНЫЕ противопоказания:**

| Вещество | Противопоказание | Где назначается | Статус |
|----------|------------------|-----------------|--------|
| Cabergoline | Фиброз сердечных клапанов, психоз, тяжёлая гипотензия | rec-eng:304 | ❌ НЕ проверяется |
| Cabergoline | Беременность | rec-eng:304 | ❌ НЕ проверяется |
| Anastrozole | Беременность, пременопауза (если женщина) | rec-eng:273 | ❌ НЕ проверяется |
| Aspirin | Язва ЖКТ, геморрагический диатез, возраст <16 (синдром Рея) | lab-priority-map:138,146 | ❌ НЕ проверяется |
| Niacin | Подагра, язва, активное заболевание печени, гипотензия | lab-priority-map:84,90,634 | ❌ НЕ проверяется |
| Red yeast rice | Активное заболевание печени, беременность, статины одновременно | lab-priority-map:80, rec-eng:357 | ❌ НЕ проверяется (опасно со статинами!) |
| Berberine | Беременность, цирроз (ингибирует CYP3A4 — взаимодействия) | lab-priority-map:78 | ❌ НЕ проверяется |
| TUDCA | Острый холецистит с обструкцией (риск усиления) | rec-eng:230 | ❌ НЕ проверяется |
| hCG | Андроген-чувствительный рак простаты, преждевременное половое созревание | rec-eng:319 | ❌ НЕ проверяется |
| Zinc | Дефицит меди (высокие дозы Zn → Cu-дефицит, анемия) | rec-eng:722 | ❌ НЕ проверяется |
| Vitamin D3 | Гиперкальциемия, саркоидоз, первичный гиперпаратиреоз | rec-eng:406,722 | ❌ НЕ проверяется |
| Vitamin K2 | Антикоагулянтная терапия (warfarin — K2 antagonist) | lab-priority-map:54,495 | ❌ НЕ проверяется — ОПАСНО |
| Ashwagandha | Аутоиммунные заболевания (тиреоидит Хашимото — ↑иммунитета), беременность | rec-eng:640,615 | ❌ НЕ проверяется |
| Berberine + Macrolides | Опасное взаимодействие (QT-удлинение, CYP3A4) | — | ❌ НЕ проверяется |
| NAC + Nitroglycerin | Опасное взаимодействие (выраженная гипотензия) | — | ❌ НЕ проверяется |
| 5-HTP + SSRI | Серотониновый синдром | rec-eng:643, lab-priority-map:68 | ❌ НЕ проверяется |
| Tyrosine + MAOI | Гипертонический криз | rec-eng:643 | ❌ НЕ проверяется |

### 5.2 Опасные комбинации (drug-drug interactions) — НЕ проверяются

Движок не имеет встроенного DDI-чекера. Конфликты проверяются только через `getConflictScore` (rec-eng:754) — это **синергетическая сеть** (SYNERGY_NETWORK), а не клинические DDI.

**Критичные пропущенные DDI:**
1. **Red yeast rice + statins** (same class — shared-constants.ts:235 дедуплицирует, но НЕ предупреждает о риске рабдомиолиза при двойной дозе)
2. **Niacin + statins** (риск миопатии — AIM-HIGH trial показало отсутствие пользы + риск)
3. **Aspirin + anticoagulants** (кровотечение — нет проверки)
4. **Berberine + CYP3A4 substrates** (множество взаимодействий — cyclosporine, макролиды)
5. **Telmisartan + potassium supplements** (гиперкалиемия — нет проверки, хотя оба могут назначаться)
6. **Nebivolol + beta-blockers** (same class — дедупликация есть, но нет предупреждения о брадикардии)
7. **hCG + AI** (hCG ↑E2 → может потребовать больше AI — нет динамической подстройки)

### 5.3 Тромбофилия — ОПАСНАЯ обработка (rec-eng:670)

```
condition: state.contraindications.hasThrombophilia
→ fallback: ['serrapeptase', 'nattokinase']
→ escalation: 'D-димер > 500 → LMWH'
```

**КРИТИЧНО:** Тромбофилия — это абсолютное противопоказание к ААС. Назначение serrapeptase/nattokinase при тромбофилии — **недостаточно**. Гайдлайны ASH 2021: тромбофилия + эритроцитоз = **антикоагуляция (LMWH/DOAC)**, не БАДы.

**Движок должен:** при тромбофилии выдавать **КРИТИЧЕСКОЕ предупреждение** — противопоказание к ААС, обязательная консультация гематолога, антикоагуляция. Текущая обработка — **медицински опасна** (даёт ложное чувство безопасности БАДами).

### 5.4 ССЗ + стимуляторы (rec-eng:671)

```
condition: state.contraindications.hasCVD || state.cardio.previousCVD
→ title: 'исключить стимуляторы, усилить кардиомониторинг'
```

**Проблема:** Движок рекомендует исключить стимуляторы, но **не блокирует их назначение**. Нет фильтра, который бы удалял stimulant-класс препаратов при hasCVD. Это только текстовое предупреждение — фактически стимуляторы могут остаться в плане.

### 5.5 Эпилепсия (rec-eng:674)

```
condition: state.contraindications.hasEpilepsy
→ title: 'исключить стимуляторы, контроль кетогенной диеты'
```

**Проблема:** Аналогично — текстовое предупреждение без блокировки. Также **пропущено:** 5-HTP, tyrosine, GABA-модуляторы могут влиять на seizure threshold. Нет проверки.

### 5.6 Психические расстройства (rec-eng:675)

```
condition: state.contraindications.hasMentalIllness
→ title: 'исключить нейротоксичные ААС, стимуляторы'
```

**Проблема:** Текст без блокировки. Также **пропущено:** ashwagandha, rhodiola, ginseng (адаптогены могут манические эпизоды при биполярке). Назначается ashwagandha (rec-eng:615) при hasMentalIllness — **противоречит** собственному предупреждению.

---

## 6. ТОЧНОСТЬ MECHANISM-BASED REASONING (mechanism-support-bridge.ts)

### 6.1 Структура маппинга (mechanism-support-bridge.ts:8-167)

`MECHANISM_TO_SUPPORT_SUBSTANCE` — маппинг 81 механизма (9 систем × 7-8 механизмов) на support-вещества.

**Архитектура:** `system_mechNumber → string[]` (например, `cardio_4 → ['serrapeptase', 'nattokinase', 'naringin', 'lumbrokinase', 'aspirin']` — «Тромбогенный потенциал»).

### 6.2 Оценка точности по системам

**CARDIO (строки 10-17):**
- `cardio_1` (Дислипидемия): omega3, bergamot, vitamin_e, berberine, niacin, red_yeast_rice — ✅ Корректно (соответствует AACE/ESC)
- `cardio_2` (Гипертензия): telmisartan, nebivolol, magnesium, diosmin, hesperidin, pycnogenol — ⚠️ Telmisartan/nebivolol — рецептурные, не БАДы. Diosmin/hesperidin — венотоники (не для АД). **Нет amlodipine** (CCB первой линии).
- `cardio_4` (Тромбогенный): serrapeptase, nattokinase, naringin, lumbrokinase, aspirin — ⚠️ Serrapeptase/nattokinase — слабая доказательная база. **Нет clopidogrel, DOACs** (апиксабан, ривароксабан) — препараты выбора для тромбопрофилактики.
- `cardio_7` (Аритмогенность): magnesium, taurine, potassium_sup, l_theanine — ✅ Корректно для мягких случаев. **Нет beta-blockers, antiarrhythmics** для серьёзных аритмий.

**HEPATIC (строки 20-27):**
- `hepatic_1` (Холестаз): tudca, milk_thistle, phosphatidylcholine, artichoke, naringin — ✅ Корректно (UDCA — препарат выбора)
- `hepatic_2` (Цитолиз): nac, milk_thistle, alpha_lipoic, tudca, glycine — ✅ Корректно (NAC — препарат выбора)
- `hepatic_8` (Фиброз/цирроз): milk_thistle, nac, tudca, alpha_lipoic — ⚠️ **Пропущен:** silymarin (уже есть как milk_thistle). Нет коллаген-ингибиторов (LOXL2-ингибиторы — экспериментальные). Для цирроза — **нет предупреждения о необходимости врача** (цирроз необратим).

**RENAL (строки 30-36):**
- `renal_1` (Гиперфильтрация): astragalus, taurine, cordyceps, d_mannose, vitamin_d3 — ⚠️ Astragalus — есть данные (RCT на CKD), но не препарат выбора. **Нет:** ACE inhibitors/ARBs (ренопротекция при протеинурии — KDIGO). Движок назначает БАДы при протеинурии — **опасно** (требуется ACEi/ARB).
- `renal_3` (ОПП): astragalus, taurine, cordyceps, coq10, alpha_lipoic — ❌ **АКИ требует медицинской помощи**, не БАДов. Движок должен направлять к врачу.

**NEURO (строки 39-55):**
- `neuro_1` (Окислительный): phosphatidylserine, alpha_lipoic, coq10, omega3 — ✅ OK
- `neuro_tox_1` (Дофамин): citicoline, fasoracetam, bromantane, lions_mane — ⚠️ Fasoracetam, bromantane — ноотропы с ограниченной доказательной базой. **Нет:** L-tyrosine (предшественник дофамина), mucuna pruriens (L-DOPA) — хотя L-DOPA в PHASE_BLOCKLIST не указан.
- `neuro_tox_7` (Сон): melatonin, ashwagandha, l_theanine, glycine — ✅ Корректно

**ENDOCRINE (строки 58-65):**
- `endocrine_1` (HPTA): zinc_sup, shilajit, tongkat_ali, boron, fadogia — ⚠️ Fadogia agrestis — **токсичность печени** (FDA warning 2023). **Не должен быть в рекомендациях.**
- `endocrine_4` (Пролактин): vitex, p5p, indinol, zinc_sup — ✅ OK для мягких случаев. **Нет cabergoline** (но он добавляется отдельно в rec-eng:304).
- `endocrine_8` (PCT): enclomiphene, tamoxifen, clomi, zinc_sup — ✅ Корректно (SERM/SERM для ПКТ)

**HEMATOLOGIC (строки 68-74):**
- `hematologic_1` (Полицитемия): serrapeptase, nattokinase, naringin, lumbrokinase, aspirin — ⚠️ При HCT >54% — **флеботомия** (стандарт), не БАДы. Движок не упоминает флеботомию в механизмах (хотя в rec-eng:681 есть «Донорство при HCT > 54»).
- `hematologic_2` (Анемия): iron_supplement, vitamin_c, folate, vitamin_b12 — ✅ Корректно

**REPRODUCTIVE (строки 77-83):**
- `reproductive_1` (Яички): enclomiphene, tamoxifen, zinc_sup, boron — ⚠️ На курсе ААС эти не работают (HPTA подавлен). Должен быть hCG (но он в rec-eng:319 отдельно).
- `reproductive_6` (Простата): saw_palmetto, zinc_sup, selenium_sup — ✅ OK для ДГПЖ. **Нет dutasteride/finasteride** при высоком ДГТ.

**METABOLIC (строки 95-102):**
- `metabolic_1` (IR): berberine, alpha_lipoic, chromium, vitamin_d3 — ✅ OK (berberine ≈ metformin по эффективности). **Нет metformin** (но он в PHARMACY_DB, назначается отдельно).
- `metabolic_8` (Гомоцистеин): vitamin_b12, methylfolate, tmg, taurine — ✅ Корректно

### 6.3 Ошибки mechanism-based reasoning

1. **`endocrine_1` содержит fadogia** (строка 58) — **опасно** (гепатотоксичность, FDA warning 2023). Должен быть удалён.

2. **`renal_1-3` предлагают БАДы для серьёзных почечных состояний** (гиперфильтрация, ОПП) — **медицински безответственно**. Требуется: при proteinuria/ОПП → направлять к нефрологу, не назначать astragalus.

3. **`cardio_4` (тромбогенный)** — пропущены настоящие антикоагулянты (DOACs). При тромбофилии БАДы недостаточны.

4. **`hematologic_1` (полицитемия)** — пропущена флеботомия (стандарт лечения при HCT >54%).

5. **`cardio_2` (гипертензия)** — diosmin/hesperidin (венотоники) не имеют места в лечении АД. Telmisartan — хорош (ARB + PPAR-γ), но должен быть препаратом выбора, а не «support».

6. **`endocrine_5` (Эстрадиол)** содержит `mesterolone` (провирон) — это ААС/DHT-производное, не «support». **Логическая ошибка** — ААС не должны быть в support-категории.

### 6.4 getFullChainSupport (строки 186-201)

`organToSystem` маппинг (строка 188-193):
- liver → hepatic, heart → cardio, kidneys → renal, brain → neuro, testes → reproductive, prostate → reproductive, blood_vessels → vessels, thyroid → thyroid, pancreas → metabolic, bones → musculoskeletal, muscles → musculoskeletal, immune → immunity, skin → skin

**Проблема:** `skin` нет в `MECHANISM_TO_SUPPORT_SUBSTANCE` (нет skin_* ключей). При `organ='skin'` → `mechanisms = []`, `supportIds = []`. **Молчаливый провал** — пользователь не получает поддержки для кожи.

### 6.5 normalizeMechanisms (rec-eng:98)

`canon = normalizeMechanisms([m])[0] || m` — нормализация названий механизмов перед lookup в `MECHANISM_TO_SUPPORT`.

**Риск:** Если `normalizeMechanisms` не покрывает все варианты написания → fallback на оригинальное название → может не найтись в `MECHANISM_TO_SUPPORT` → пустой результат. Нет валидации/логирования пропусков.

---

## 7. ИТОГОВАЯ ОЦЕНКА И ПРИОРИТЕТНЫЕ ИСПРАВЛЕНИЯ

### 7.1 Сводная оценка по медицинским критериям

| Критерий | Оценка | Комментарий |
|----------|--------|-------------|
| Соответствие порогов гайдлайнам 2021+ | ⚠️ ЧАСТИЧНОЕ | ALT/AST/GGT/LDL/HCT/VitD/B12 устарели |
| Логика mandatory назначений | ⚠️ ЧАСТИЧНО | hCG/AI/cabergoline триггеры обоснованы, но превентивные без лаб. подтверждения — преждевременны |
| Покрытие противопоказаний | ❌ НЕДОСТАТОЧНОЕ | 14+ критичных противопоказаний не проверяются |
| DDI-чекер | ❌ ОТСУТСТВУЕТ | Только синергетическая сеть, не клинические DDI |
| Нормализация единиц | ❌ ОПАСНАЯ | E2/Cortisol/DHEA-S эвристики могут ложно конвертировать |
| Mechanism-based reasoning | ⚠️ ЧАСТИЧНОЕ | Fadogia (гепатотоксичен), mesterolone (не support), пропуски DOACs/флеботомии |
| Severity-фильтрация | ⚠️ РАБОТАЕТ, НО ПРОИЗВОЛЬНА | deriveSeverity ×2/×3 — не клинически валидированы |
| Дифференциация по полу/возрасту | ❌ ОТСУТСТВУЕТ | Ferritin, HDL, PSA, DHEA-S, D-dimer — без учёта демографии |
| Покрытие фаз (course/PCT/bridge) | ✅ ХОРОШО | PHASE_BLOCKLIST корректно блокирует AI на ПКТ |

### 7.2 Приоритет 1 — КРИТИЧНЫЕ исправления (безопасность пациента)

1. **CABERGOLINE противопоказания** (rec-eng:304):
   - Добавить проверку: клапанный фиброз, психоз, гипотензия
   - При наличии → блокировать + направлять к эндокринологу

2. **ТРОМБОФИЛИЯ обработка** (rec-eng:670):
   - Заменить fallback БАДы на: «⚠ ПРОТИВОПОКАЗАНИЕ к ААС. Консультация гематолога. Антикоагуляция по показаниям.»
   - Не назначать serrapeptase/nattokinase как «достаточные»

3. **VITAMIN K2 + anticoagulants** (warfarin):
   - Добавить проверку concurrent anticoagulant therapy
   - При наличии → блокировать K2 (антагонист warfarin)

4. **ASPIRIN противопоказания** (lab-priority-map:138,146):
   - Добавить: язва ЖКТ, геморрагический диатез, возраст <16
   - Не назначать при <40 лет без CV-риска

5. **RED YEAST RICE + statins** (rec-eng:357, shared-constants:235):
   - Дедупликация есть, но добавить **предупреждение** о риске рабдомиолиза при комбинации

6. **FADOGIA удалить** из mechanism-support-bridge.ts:58 (гепатотоксичность, FDA 2023)

### 7.3 Приоритет 2 — ВЫСОКИЕ исправления (точность назначений)

7. **Пороги ALT/AST** (rec-eng:498-499): 40 → 30 (M)/25 (F) по AASLD 2021

8. **Порог HCT триггера** (rec-eng:203): 47 → 50 (M), 44 → 46 (F). Severe → 54 (флеботомия)

9. **Порог LDL** (rec-eng:506): 3.0 → 2.6 (ААС-пользователи = высокий риск по ESC)

10. **Порог Vitamin D** (rec-eng:403,410): 30 → 20 (дефицит), 30 = «недостаточность» (не дефицит)

11. **Нормализация E2** (constants.ts:585-591): убрать эвристику >50, использовать явное указание единиц пользователем

12. **Нормализация Cortisol/DHEA-S** (constants.ts:609-621): аналогично — требовать явные единицы

13. **Возрастная корректировка PSA** (rec-eng:519): 40-49 → 2.5; 50-59 → 3.0; 60-69 → 4.0; 70-79 → 4.5

14. **Возрастная корректировка D-dimer** (rec-eng:511): для >50 лет — age×0.01

15. **Половая дифференцировка Ferritin** (rec-eng:435): M >300, F (premeno) >150, F (post) >200

### 7.4 Приоритет 3 — СРЕДНИЕ исправления (полнота)

16. **hCG дозировка** (rec-eng:321): адаптировать по дозе ААС (>500 мг/нед тест → 750-1000 МЕ 2×/нед)

17. **AI триггер** (rec-eng:266): убрать `maxAro > 0` как самостоятельный триггер; только по лаб. E2

18. **Cabergoline триггер** (rec-eng:297): убрать `has19Nor` как самостоятельный триггер; только по лаб. Prolactin >25

19. **Схема hCG 3/1** (rec-eng:321): пометить как «экспериментальная, непрерывный приём — стандарт»

20. **Coverage-фильтр для critical** (rec-eng:746): critical-рекомендации должны bypass basic/mid фильтр

21. **DDI-чекер**: добавить клинические DDI (не только синергии)

22. **Mesterolone** (mechanism-support-bridge.ts:62): удалить из support (это ААС, не support)

23. **Skin** (mechanism-support-bridge.ts:192): добавить skin_* механизмы или явно вернуть «не поддерживается»

24. **Алергия на вещества**: сейчас `blockedIds` (rec-eng:118) — это только пользовательские негативные реакции, но нет явной аллергии. Добавить `state.contraindications.allergies` парсинг в blockedIds

### 7.5 Приоритет 4 — НИЗКИЕ (улучшение UX)

25. **Мониторинг краша E2** (rec-eng): при AI-терапии добавить рекомендацию «при E2 <10 прекратить AI»

26. **Макропролактин**: при Prolactin >40 добавить рекомендацию проверки макропролактина

27. **Hy's law** (rec-eng:225): при ALT >3×ULN + билирубин >2×ULN → critical + немедленная отмена препарата

28. **Возрастные нормы тестостерона** (constants.ts:400): DYNAMIC_REFS.TT уже есть ageFactor, но не используется в rec-eng

29. **Добавить GFR-фильтр**: при eGFR <60 — корректировать дозы (NAC, berberine, magnesium)

---

## 8. КЛЮЧЕВЫЕ ЦИТАТЫ КОДА (для разработчиков)

### Критичные строки для исправления:

**Нормализация E2 (опасная эвристика):**
```
constants.ts:590: if (rawValue > 50) return Math.round(rawValue / 3.67 * 10) / 10;
```

**Тромбофилия — БАДы вместо антикоагулянтов:**
```
rec-eng:670: fallback:['serrapeptase','nattokinase']
rec-eng:670: escalation:'D-димер > 500 → LMWH'
```

**hCG 3/1 схема (невалидированная):**
```
rec-eng:321: 'ХГЧ 500 МЕ 2р/нед, схема 3/1 (3 нед приём, 1 нед отдых)'
```

**Cabergoline без противопоказаний:**
```
rec-eng:304-309: if (maxProg >= 0.4 || has19Nor) { ids.push('cabergoline'); }
```

**Vitamin K2 без проверки anticoagulants:**
```
lab-priority-map.ts:495: { substanceId: 'vitamin_k2', ... reason: 'Активация факторов свёртывания II/VII/IX/X' }
```

**Fadogia (гепатотоксичен):**
```
mechanism-support-bridge.ts:58: endocrine_1: [...'fadogia']
```

**Устаревший порог ALT:**
```
rec-eng:498: { marker:'ALT', ... threshold:{ higherIsWorse:true, value:40 }
```

**Покрытие противопоказаний — неполное:**
```
engine-helpers.ts:806-815: getContraindicationAlerts — только 5 проверок
```

**Coverage-фильтр удаляет антигипертензивные при basic:**
```
engine-helpers.ts:604: for (const e of ['telmisartan', 'nebivolol', ...]) all.delete(e);
```

---

## 9. ЗАКЛЮЧЕНИЕ

Движок recommendation-engine.ts имеет **хорошую архитектуру** (4 фазы, priority-map, mechanism-bridge, same-class дедупликация, phase-blocklist), но **медицинская точность требует существенной доработки**:

**Сильные стороны:**
- Чёткая структура: marker → priority → substance → reasoning
- Каскадная severity-фильтрация (mild → moderate → severe)
- PHASE_BLOCKLIST корректно блокирует AI на ПКТ, Т-бустеры на курсе
- Same-class дедупликация (shared-constants.ts:232-257)
- Body-weight dosing (engine-helpers.ts:31-34, аллометрическое масштабирование Клейбера)

**Слабые стороны:**
- 14+ критичных противопоказаний не проверяются
- Пороги лабораторных отклонений устарели (ALT, AST, GGT, LDL, HCT, VitD, B12)
- Нормализация единиц (E2, Cortisol, DHEA-S) — эвристическая, риск ложной конвертации
- Нет половой/возрастной дифференцировки (Ferritin, PSA, D-dimer, DHEA-S, HDL)
- Тромбофилия обрабатывается БАДами — медицински опасно
- Нет DDI-чекера (только синергетическая сеть)
- Mechanism-bridge содержит опасные вещества (fadogia, mesterolone)
- Critical-рекомендации могут быть удалены coverage-фильтром

**РЕКОМЕНДАЦИЯ:** Движок НЕ готов для автономных медицинских назначений. Требуется реализация Приоритетов 1-2 (безопасность + точность) перед любым clinical deployment. Все назначения должны сопровождаться дисклеймером «требует верификации врачом».

---
*Аудит завершён. Файл: D:\BodyBuildHealth\MEDICAL_AUDIT_REPORT.md*
