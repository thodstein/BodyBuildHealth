import type { SRCycleTemplate, SRDaySpec, SRSetSpec, SRExerciseSpec } from '../lms-types';

// Соловьёв жим 28 дней. 2-волновая периодизация (14дн × 2). PM=100 (опорный).
// 9 жимовых сессий за 28 дней: средняя(4-6пвт)×3, лёгкая(6-20пвт)×3, тяжёлая(2-3пвт), тяжёлая(синглы).
// Дни спины+бицепса (2,7,16) не моделируются (отдых от жима).
// Формат колонок: чётные=%ПМ, нечётные=повторы или диапазон "-".
// "N-M" → mid-range rounded. Разминочные в тяжёлых сессиях исключены в источнике — добавлены стандартные.

const s = (pct: number, reps: number, sets = 1): SRSetSpec => ({ pct, reps, sets });
const ex = (name: string, group: string, coef: number, sets: SRSetSpec[]): SRExerciseSpec => ({ name, group, coef, mnosz: 1, sets });
const day = (...exercises: SRExerciseSpec[]): SRDaySpec => ({ exercises });

const BP = (sets: SRSetSpec[]) => ex('Жим лёжа', 'Жим', 1.0, sets);

// Полноценная ПЛ-структура: спина + плечи + пресс после основного жима
const row = ex('Тяга штанги в наклоне', 'Спина', 0.5, [s(.50,8,3)]);
const ohp = ex('Жим стоя', 'Плечи', 0.5, [s(.50,6,3)]);
const abs = ex('Пресс', 'Пресс', 0, [s(.15,15,3)]);

const weeks: SRDaySpec[][] = [
 // ── Неделя 1: средняя (D4) ──
 [
  day(BP([s(.20,8), s(.30,6), s(.40,4), s(.50,4), s(.60,4), s(.67,4), s(.70,4), s(.725,4), s(.65,7), s(.40,10)]]), row, ohp, abs),
 ],
 // ── Неделя 2: средняя (D8), лёгкая (D11), тяжёлая 2-3 (D14) ──
 [
  day(BP([s(.20,8), s(.30,6), s(.40,4), s(.50,4), s(.60,4), s(.67,4), s(.70,4), s(.75,4), s(.80,4), s(.685,7), s(.485,14)]]), row, ohp, abs),
  day(BP([s(.20,8), s(.30,6), s(.40,4), s(.46,6), s(.335,20)]]), row, ohp, abs),
  day(BP([s(.20,8), s(.35,6), s(.50,4), s(.65,3), s(.75,3), s(.80,3), s(.82,3), s(.85,3), s(.87,3), s(.90,3), s(.775,6)]]), row, ohp, abs),
 ],
 // ── Неделя 3: лёгкая (D17), средняя (D19) ──
 [
  day(BP([s(.20,8), s(.27,6), s(.34,6), s(.37,4), s(.41,4), s(.45,4), s(.34,15), s(.27,20)]]), row, ohp, abs),
  day(BP([s(.20,8), s(.30,6), s(.40,4), s(.50,4), s(.60,4), s(.67,4), s(.70,4), s(.725,4), s(.75,4), s(.60,8), s(.40,15)]]), row, ohp, abs),
 ],
 // ── Неделя 4: лёгкая (D22), разминка (D25), тяжёлая синглы (D28) ──
 [
  day(BP([s(.20,8), s(.30,6), s(.40,6), s(.46,6), s(.55,6), s(.40,15), s(.30,20)]]), row, ohp, abs),
  day(BP([s(.20,8), s(.30,6), s(.40,6), s(.46,4), s(.40,4), s(.30,15)]]), row, ohp, abs),
  day(BP([s(.20,8), s(.35,5), s(.50,3), s(.65,2), s(.75,1), s(.86,1), s(.91,1), s(.96,1), s(1,1), s(.835,5)]]), row, ohp, abs),
 ],
];

export const SRC2_SOLOVYOV_BENCH_28: SRCycleTemplate = {
 meta: {
  id: 'src2-solovyov-bench-28',
  title: 'Соловьёв жим 28 дней',
  direction: 'bench',
  level: 'MS-MSMK',
  period: 'peak',
  minBodyWeight: 75,
  sessionsPerWeek: 3,
  weeks: 4,
  correctionPct: 0,
  description: '28-дневная программа специализации жима лёжа Вячеслава Соловьёва. Двойная волновая периодизация (2 волны по ~14 дней): средние (4-6 пвт) + лёгкие (6-20 пвт) + тяжёлые (2-3 пвт → синглы 85-100%). Микроцикл 1: средняя→средняя→лёгкая→тяжёлая (2-3 пвт). Микроцикл 2: лёгкая→средняя→лёгкая→разминка→тяжёлая (синглы до 100%). PM=100 опорный.',
  howItWorks: 'Соловьёв жим 28 дн, 2-волновая периодизация для соревновательного цикла. Волна 1 (D4-D14): средние на 4-6 пвт (67-80%), лёгкая на 6-20 пвт (32-47%), тяжёлая на 2-3 пвт (75-90% + backdown 75-80%/6). Волна 2 (D17-D28): повторная лёгкая→средняя→лёгкая→разминка (та же структура на 2-7.5 кг тяжелее), финал — синглы 85-100%. Принцип: на тяжёлой — полная выкладка, на средних и лёгких — отдых и восстановление. Спина+бицепс (D2,7,16) для восстановительного объёма. Соревновательный проходной вес не обязан быть 100% — успехом считается 97-98% от плана.',
  conditions: ['Опытные жимовики (МС+)', '2-волновая 14-дн периодизация', 'Специализация жима', 'Средние+лёгкие+тяжёлые'],
  tags: ['solovyov'],
 },
 week1: weeks[0],
 weeks,
};
